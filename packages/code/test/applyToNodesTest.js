/**
 * Test for applyToNodes.
 * Runs with mocha.
 */
'use strict'

describe('apply-to-nodes', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
