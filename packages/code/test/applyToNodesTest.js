'use strict'
/**
 * @file Test for applyToNodes.
 * Runs with mocha.
 */
describe('apply-to-nodes', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
