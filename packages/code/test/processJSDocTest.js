/**
 * Test for processJSDoc.
 * Runs with mocha.
 */
'use strict'

describe('process-js-doc', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {})
})

/* global describe, before, after, it */
