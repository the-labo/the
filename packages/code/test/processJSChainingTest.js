/**
 * Test for processJSChaining.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const processJSChaining = require('../lib/processors/processJSChaining')

describe('process-js-chaining', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(processJSChaining)
  })
})

/* global describe, before, after, it */
