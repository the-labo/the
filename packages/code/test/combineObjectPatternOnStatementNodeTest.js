/**
 * Test for combineObjectPatternOnStatementNode.
 * Runs with mocha.
 */
'use strict'

describe('combine-object-pattern-on-statement-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
