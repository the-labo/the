/**
 * @file Test for calcNumericOperationOnBinaryExpressionNode.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const calcNumericOperationOnBinaryExpressionNode = require('../lib/ast/nodes/calcNumericOperationOnBinaryExpressionNode')

describe('calc-numeric-operation-on-binary-expression-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(calcNumericOperationOnBinaryExpressionNode)
  })
})

/* global describe, before, after, it */
