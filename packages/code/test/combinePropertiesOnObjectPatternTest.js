/**
 * @file Test for combinePropertiesOnObjectPattern.
 * Runs with mocha.
 */
'use strict'

describe('combine-properties-on-object-pattern', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
