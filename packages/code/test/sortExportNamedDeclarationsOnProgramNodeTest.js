/**
 * @file Test for sortExportNamedDeclarationsOnProgramNode.
 * Runs with mocha.
 */
'use strict'

describe('sort-export-named-declarations-on-program-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
