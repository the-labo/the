'use strict'
/**
 * Test for astHelper.
 * Runs with mocha.
 */
describe('ast-helper', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
