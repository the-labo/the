/**
 * Test for astHelper.
 * Runs with mocha.
 */
'use strict'

describe('ast-helper', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
