/**
 * Test for astHelper.
 * Runs with mocha.
 */
'use strict'

const astHelper = require('../lib/helpers/astHelper')
const { ok, equal, deepEqual } = require('assert').strict

describe('ast-helper', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
