/**
 * @file Test for modifyToDestructorOnDeclarationNode.
 * Runs with mocha.
 */
'use strict'

describe('modify-to-destructor-on-declaration-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
