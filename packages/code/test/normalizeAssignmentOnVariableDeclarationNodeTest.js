/**
 * Test for normalizeAssignmentOnVariableDeclarationNode.
 * Runs with mocha.
 */
'use strict'

describe('normalize-assignment-on-variable-declaration-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
