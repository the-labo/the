'use strict'
/**
 * Test for applyConverter.
 * Runs with mocha.
 */
describe('apply-converter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
