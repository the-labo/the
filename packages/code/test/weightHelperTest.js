/**
 * Test for weightHelper.
 * Runs with mocha.
 */
'use strict'

describe('weight-helper', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
