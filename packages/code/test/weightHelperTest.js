/**
 * Test for weightHelper.
 * Runs with mocha.
 */
'use strict'

const weightHelper = require('../lib/helpers/weightHelper')
const { ok, equal } = require('assert')

describe('weight-helper', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
