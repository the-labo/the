/**
 * Test for arrayHelper.
 * Runs with mocha.
 */
'use strict'

const arrayHelper = require('../lib/helpers/arrayHelper')
const { ok, equal } = require('assert')

describe('array-helper', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
