/**
 * Test for arrayHelper.
 * Runs with mocha.
 */
'use strict'

describe('array-helper', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
