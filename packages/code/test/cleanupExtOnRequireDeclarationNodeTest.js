/**
 * Test for cleanupExtOnRequireDeclarationArgumentNode.
 * Runs with mocha.
 */
'use strict'

const cleanupExtOnRequireDeclarationArgumentNode = require('../lib/ast/nodes/cleanupExtOnRequireDeclarationArgumentNode')
const { ok, equal, deepEqual } = require('assert').strict

describe('cleanup-ext-on-require-declaration-node', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
