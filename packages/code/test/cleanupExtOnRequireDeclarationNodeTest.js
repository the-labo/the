'use strict'
/**
 * @file Test for cleanupExtOnRequireDeclarationArgumentNode.
 * Runs with mocha.
 */
describe('cleanup-ext-on-require-declaration-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
