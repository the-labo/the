'use strict'
/**
 * Test for processPackageLockJSON.
 * Runs with mocha.
 */
describe('process-package-lock-jso-n', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
