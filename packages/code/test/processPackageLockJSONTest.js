/**
 * Test for processPackageLockJSON.
 * Runs with mocha.
 */
'use strict'

describe('process-package-lock-jso-n', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
