/**
 * Test for addExtOnRequireDeclarationArgumentNode.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const addExtOnRequireDeclarationArgumentNode = require('../lib/ast/nodes/addExtOnRequireDeclarationArgumentNode')

describe('add-ext-on-require-declaration-argument-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(addExtOnRequireDeclarationArgumentNode)
  })
})

/* global describe, before, after, it */
