/**
 * Test for FormatCache.
 * Runs with mocha.
 */
'use strict'

describe('format-cache', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
