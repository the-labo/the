/**
 * Test for combineOnVariableDeclarationNodes.
 * Runs with mocha.
 */
'use strict'

describe('combine-on-variable-declaration-nodes', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
