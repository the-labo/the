'use strict'
/**
 * Test for spaceOnCommentNode.
 * Runs with mocha.
 */
describe('space-on-comment-node', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
