/**
 * Test for processFileEnd.
 * Runs with mocha.
 */
'use strict'

describe('process-file-end', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
