/**
 * Test for findCJSExportsAssignmentsExpressionOnProgramNode.
 * Runs with mocha.
 */
'use strict'

const findCJSExportsAssignmentsExpressionOnProgramNode = require('../lib/ast/nodes/findCJSExportsAssignmentsExpressionOnProgramNode')
const { ok, equal, deepEqual } = require('assert').strict

describe('find-cjs-exports-assignments-expression-on-program-node', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
