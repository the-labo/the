/**
 * Test for TypeNamings.
 * Runs with mocha.
 */
'use strict'

describe('type-namings', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
