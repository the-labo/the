'use strict'
/**
 * Test for TypeNamings.
 * Runs with mocha.
 */
describe('type-namings', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
