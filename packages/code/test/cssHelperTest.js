/**
 * Test for cssHelper.
 * Runs with mocha.
 */
'use strict'

describe('css-helper', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
