/**
 * Test for TheGeo.
 * Runs with mocha.
 */
'use strict'

const TheGeo = require('../lib/TheGeo')
const { ok, equal } = require('assert')

describe('the-geo', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(TheGeo)
  })
})

/* global describe, before, after, it */
