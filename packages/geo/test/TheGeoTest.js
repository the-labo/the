'use strict'
/**
 * Test for TheGeo.
 * Runs with mocha.
 */
const { ok } = require('assert').strict
const TheGeo = require('../lib/TheGeo')

describe('the-geo', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(TheGeo)
  })
})

/* global describe, before, after, it */
