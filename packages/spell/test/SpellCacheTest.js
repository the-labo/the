/**
 * Test for SpellCache.
 * Runs with mocha.
 */
'use strict'

describe('spell-cache', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
