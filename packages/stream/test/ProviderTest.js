'use strict'
/**
 * Test for Provider.
 * Runs with mocha.
 */
describe('provider', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
