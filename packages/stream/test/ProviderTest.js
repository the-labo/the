/**
 * Test for Provider.
 * Runs with mocha.
 */
'use strict'

describe('provider', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
