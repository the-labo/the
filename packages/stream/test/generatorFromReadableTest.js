/**
 * Test for generatorFromReadable.
 * Runs with mocha.
 */
'use strict'

describe('generator-from-readable', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
