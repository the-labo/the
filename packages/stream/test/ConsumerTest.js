'use strict'
/**
 * Test for Consumer.
 * Runs with mocha.
 */
describe('consumer', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
