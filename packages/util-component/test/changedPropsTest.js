/**
 * Test for changedProps.
 * Runs with mocha.
 */
'use strict'

describe('changed-props', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
