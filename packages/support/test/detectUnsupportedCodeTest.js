/**
 * Test for detectUnsupportedCode.
 * Runs with mocha.
 */
'use strict'

describe('detect-unsupported-code', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
