'use strict'
/**
 * Test for detectUnsupportedCode.
 * Runs with mocha.
 */
describe('detect-unsupported-code', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
