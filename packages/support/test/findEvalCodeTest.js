/**
 * Test for findEvalCode.
 * Runs with mocha.
 */
'use strict'

const findEvalCode = require('../lib/helpers/findEvalCode')

const { ok, equal, deepEqual } = require('assert').strict

describe('find-eval-code', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
