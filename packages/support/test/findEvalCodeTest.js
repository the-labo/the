'use strict'
/**
 * Test for findEvalCode.
 * Runs with mocha.
 */
describe('find-eval-code', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
