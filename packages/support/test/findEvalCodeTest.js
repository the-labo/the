/**
 * Test for findEvalCode.
 * Runs with mocha.
 */
'use strict'

describe('find-eval-code', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
