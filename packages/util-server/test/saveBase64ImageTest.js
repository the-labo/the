/**
 * Test for saveBase64Image.
 * Runs with mocha.
 */
'use strict'

describe('save-base64-image', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
