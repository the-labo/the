/**
 * Test for SFUProxy.
 * Runs with mocha.
 */
'use strict'

const SFUProxy = require('../lib/sfu/SFUProxy')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('s-f-u-proxy', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
