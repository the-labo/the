'use strict'
/**
 * Test for SFUProxy.
 * Runs with mocha.
 */
describe('s-f-u-proxy', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
