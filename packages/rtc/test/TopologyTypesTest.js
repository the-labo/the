/**
 * Test for TopologyTypes.
 * Runs with mocha.
 */
'use strict'

describe('topology-types', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
