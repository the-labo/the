/**
 * Test for IOEvents.
 * Runs with mocha.
 */
'use strict'

describe('i-o-events', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
