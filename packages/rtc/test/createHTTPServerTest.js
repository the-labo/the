/**
 * Test for createHTTPServer.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const createHTTPServer = require('../lib/helpers/createHTTPServer')

describe('create-http-server', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(createHTTPServer)
  })
})

/* global describe, before, after, it */
