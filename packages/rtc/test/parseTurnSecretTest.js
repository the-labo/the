'use strict'
/**
 * Test for parseTurnSecret.
 * Runs with mocha.
 */
describe('parse-turn-secret', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
