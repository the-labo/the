/**
 * Test for parseTurnSecret.
 * Runs with mocha.
 */
'use strict'

const parseTurnSecret = require('../lib/helpers/parseTurnSecret')
const { ok, equal, deepEqual } = require('assert').strict

describe('parse-turn-secret', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
