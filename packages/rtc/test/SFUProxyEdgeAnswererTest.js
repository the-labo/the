'use strict'
/**
 * Test for SFUProxyEdgeAnswerer.
 * Runs with mocha.
 */
describe('s-f-u-proxy-edge-answerer', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
