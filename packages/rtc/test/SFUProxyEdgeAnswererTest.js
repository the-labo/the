/**
 * Test for SFUProxyEdgeAnswerer.
 * Runs with mocha.
 */
'use strict'

describe('s-f-u-proxy-edge-answerer', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
