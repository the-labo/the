/**
 * Test for IOConnector.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const IOConnector = require('../lib/helpers/IOConnector')

describe('io-connector', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(IOConnector)
  })
})

/* global describe, before, after, it */
