/**
 * Test for SFUProxyPool.
 * Runs with mocha.
 */
'use strict'

describe('s-f-u-proxy-pool', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
