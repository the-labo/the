/**
 * Test for SFUProxyPool.
 * Runs with mocha.
 */
'use strict'

const SFUProxyPool = require('../lib/sfu/SFUProxyPool')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('s-f-u-proxy-pool', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
