/**
 * Test for ChannelNames.
 * Runs with mocha.
 */
'use strict'

describe('channel-names', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
