/**
 * Test for SFUProxyEdge.
 * Runs with mocha.
 */
'use strict'

const SFUProxyEdge = require('../lib/sfu/SFUProxyEdge')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('s-f-u-proxy-edge', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
