'use strict'
/**
 * Test for SFUProxyEdge.
 * Runs with mocha.
 */
describe('s-f-u-proxy-edge', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
