/**
 * Test for SFUProxyEdgeOfferer.
 * Runs with mocha.
 */
'use strict'

describe('s-f-u-proxy-edge-offerer', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
