'use strict'
/**
 * Test for SFUProxyEdgeOfferer.
 * Runs with mocha.
 */
describe('s-f-u-proxy-edge-offerer', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
