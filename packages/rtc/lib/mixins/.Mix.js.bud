'use strict'

module.exports = ['http', 'io', 'sfu'].map((name) => ({
  data: {
    name,
  },
  force: false,
  mode: '644',
  path: `${name}Mix.js`,
}))

if (!module.parent) {
  require('coz').render(__filename)
}
