'use strict'

module.exports = [
  'http',
  'io',
  'turn',
  'sfu',
].map((name) => ({
  force: false,
  mode: '644',
  data: {
    name,
  },
  path: `${name}Mix.js`,
}))

if (!module.parent) {
  require('coz').render(__filename)
}
