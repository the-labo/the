'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  annotations: {
    memberof: 'module:@the-/rtc',
    description: 'Helpers',
    namespace: 'helpers',
  },
  dirname: __dirname,
})

if (!module.parent) {
  require('coz').render(__filename)
}
