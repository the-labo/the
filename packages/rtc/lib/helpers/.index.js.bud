'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  annotations: {
    description: 'Helpers',
    memberof: 'module:@the-/rtc',
    namespace: 'helpers',
  },
  cjs: true,
  dirname: __dirname,
})

if (!module.parent) {
  require('coz').render(__filename)
}
