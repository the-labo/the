'use strict'
/**
 * Test for toStoreScopeClass.
 * Runs with mocha.
 */
describe('to-store-scope-class', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
