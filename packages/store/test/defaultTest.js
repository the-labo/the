/**
 * Test for default.
 * Runs with mocha.
 */
'use strict'

const default_ = require('../lib/default')
const { ok, equal } = require('assert')

describe('default', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
