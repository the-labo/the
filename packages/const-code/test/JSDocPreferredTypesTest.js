/**
 * Test for JSDocPreferredTypes.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const JSDocPreferredTypes = require('../lib/JSDocPreferredTypes')

describe('js-doc-preferred-types', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(JSDocPreferredTypes)
  })
})

/* global describe, before, after, it */
