/**
 * Test for instanceMethodNamesFor.
 * Runs with mocha.
 */
'use strict'

describe('instance-method-names-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
