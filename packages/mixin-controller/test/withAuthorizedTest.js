'use strict'
/**
 * Test for withAuthorized.
 * Runs with mocha.
 */
describe('with-authorized', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
