/**
 * Test for withAuthorized.
 * Runs with mocha.
 */
'use strict'

describe('with-authorized', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
