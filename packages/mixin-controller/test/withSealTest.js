/**
 * Test for withSeal.
 * Runs with mocha.
 */
'use strict'

describe('with-seal', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
