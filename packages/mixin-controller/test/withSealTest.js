/**
 * Test for withSeal.
 * Runs with mocha.
 */
'use strict'

const withSeal = require('../lib/withSeal')
const { ok, equal } = require('assert')

describe('with-seal', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
