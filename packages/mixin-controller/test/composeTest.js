'use strict'
/**
 * Test for compose.
 * Runs with mocha.
 */
describe('compose', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
