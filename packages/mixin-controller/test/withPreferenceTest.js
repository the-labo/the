'use strict'
/**
 * Test for withPreference.
 * Runs with mocha.
 */
describe('with-preference', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
