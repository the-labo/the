'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  description: 'Count implementations',
  dirname: __dirname,
  name: 'counters',
})

if (!module.parent) {
  require('coz').render(__filename)
}
