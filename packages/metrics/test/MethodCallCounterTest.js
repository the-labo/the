/**
 * Test for MethodCallCounter.
 * Runs with mocha.
 */
'use strict'

describe('method-call-counter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
