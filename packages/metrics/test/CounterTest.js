/**
 * Test for Counter.
 * Runs with mocha.
 */
'use strict'

describe('counter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
