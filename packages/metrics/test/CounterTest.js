'use strict'
/**
 * Test for Counter.
 * Runs with mocha.
 */
describe('counter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
