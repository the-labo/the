/**
 * Test for ObjectMethodCallCounter.
 * Runs with mocha.
 */
'use strict'

describe('object-method-call-counter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
