/**
 * Test for ClassMethodCallCounter.
 * Runs with mocha.
 */
'use strict'

describe('class-method-call-counter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
