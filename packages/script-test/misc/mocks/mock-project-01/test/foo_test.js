'use strict'

const {
  strict: { equal },
} = require('assert')

describe('build', function() {
  this.timeout(3000)

  before(() => {})

  after(() => {})

  it('test', () => {
    equal(1 + 2, 3)
  })
})

/* global describe, before, after, it */
