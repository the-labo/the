'use strict'
/**
 * Test for mongoMix.
 * Runs with mocha.
 */
describe('mongo-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
