/**
 * Test for showNotification.
 * @file Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const showNotification = require('../lib/showNotification')

describe('show-notification', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(showNotification)
  })
})

/* global describe, before, after, it */
