/**
 * Test for askNotification.
 * @file Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const askNotification = require('../lib/askNotification')

describe('ask-notification', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(askNotification)
  })
})

/* global describe, before, after, it */
