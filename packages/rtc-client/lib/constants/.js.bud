'use strict'

const constants = require('@the-/rtc/lib/constants')

module.exports = ['IOEvents', 'PeerEvents', 'ChannelNames'].map((name) => ({
  data: {
    name,
    values: constants[name],
  },
  force: true,
  mode: '444',
  path: `${name}.js`,
}))
if (!module.parent) {
  require('coz').render(__filename)
}
