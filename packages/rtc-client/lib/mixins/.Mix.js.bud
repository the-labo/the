'use strict'

module.exports = [
  'assert',
  'media',
  'serialize',
  'peer',
  'socket',
  'promise',
  'channel',
].map((name) => ({
  data: {
    name,
  },
  force: false,
  mode: '644',
  path: `${name}Mix.js`,
}))

if (!module.parent) {
  require('coz').render(__filename)
}
