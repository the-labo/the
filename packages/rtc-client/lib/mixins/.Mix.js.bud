'use strict'

module.exports = [
  'assert',
  'media',
  'serialize',
  'peer',
  'socket',
  'promise',
  'channel',
].map((name) => ({
  force: false,
  mode: '644',
  data: {
    name,
  },
  path: `${name}Mix.js`,
}))

if (!module.parent) {
  require('coz').render(__filename)
}
