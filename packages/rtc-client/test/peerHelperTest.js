/**
 * Test for peerHelper.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const peerHelper = require('../lib/helpers/peerHelper')

describe('peer-helper', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(peerHelper)
  })
})

/* global describe, before, after, it */
