'use strict'
/**
 * Test for PeerPurposes.
 * Runs with mocha.
 */
describe('peer-purposes', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
