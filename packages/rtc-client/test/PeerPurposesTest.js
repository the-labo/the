/**
 * Test for PeerPurposes.
 * Runs with mocha.
 */
'use strict'

const PeerPurposes = require('../lib/constants/PeerPurposes')
const { ok, equal } = require('assert')

describe('peer-purposes', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
