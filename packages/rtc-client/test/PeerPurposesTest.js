/**
 * Test for PeerPurposes.
 * Runs with mocha.
 */
'use strict'

describe('peer-purposes', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
