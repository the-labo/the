/**
 * Test for channelMix.
 * Runs with mocha.
 */
'use strict'

describe('channel-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
