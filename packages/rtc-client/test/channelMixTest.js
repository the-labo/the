/**
 * Test for channelMix.
 * Runs with mocha.
 */
'use strict'

const channelMix = require('../lib/mixins/channelMix')
const { ok, equal } = require('assert')

describe('channel-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
