/**
 * Test for assertMix.
 * Runs with mocha.
 */
'use strict'

const assertMix = require('../lib/mixins/assertMix')
const { ok, equal } = require('assert')

describe('assert-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
