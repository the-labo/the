/**
 * Test for assertMix.
 * Runs with mocha.
 */
'use strict'

describe('assert-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
