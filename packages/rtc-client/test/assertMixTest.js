'use strict'
/**
 * Test for assertMix.
 * Runs with mocha.
 */
describe('assert-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
