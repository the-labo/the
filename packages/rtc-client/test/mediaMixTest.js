'use strict'
/**
 * Test for mediaMix.
 * Runs with mocha.
 */
describe('media-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
