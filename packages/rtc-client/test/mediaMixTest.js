/**
 * Test for mediaMix.
 * Runs with mocha.
 */
'use strict'

const mediaMix = require('../lib/mixins/mediaMix')
const { ok, equal } = require('assert')

describe('media-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
