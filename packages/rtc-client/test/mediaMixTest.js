/**
 * Test for mediaMix.
 * Runs with mocha.
 */
'use strict'

describe('media-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
