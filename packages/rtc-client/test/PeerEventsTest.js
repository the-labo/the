/**
 * Test for PeerEvents.
 * Runs with mocha.
 */
'use strict'

describe('peer-events', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
