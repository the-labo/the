'use strict'
/**
 * Test for PeerEvents.
 * Runs with mocha.
 */
describe('peer-events', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
