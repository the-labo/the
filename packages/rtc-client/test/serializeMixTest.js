/**
 * Test for serializeMix.
 * Runs with mocha.
 */
'use strict'

const serializeMix = require('../lib/mixins/serializeMix')
const { ok, equal } = require('assert')

describe('serialize-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
