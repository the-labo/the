'use strict'
/**
 * Test for peerMix.
 * Runs with mocha.
 */
describe('peer-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
