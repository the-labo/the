/**
 * Test for peerMix.
 * Runs with mocha.
 */
'use strict'

describe('peer-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
