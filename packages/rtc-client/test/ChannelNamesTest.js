/**
 * Test for ChannelNames.
 * Runs with mocha.
 */
'use strict'

const ChannelNames = require('../lib/constants/ChannelNames')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('channel-names', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
