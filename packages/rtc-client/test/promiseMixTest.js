'use strict'
/**
 * Test for promiseMix.
 * Runs with mocha.
 */
describe('promise-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
