/**
 * Test for promiseMix.
 * Runs with mocha.
 */
'use strict'

const promiseMix = require('../lib/mixins/promiseMix')
const { ok, equal } = require('assert')

describe('promise-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
