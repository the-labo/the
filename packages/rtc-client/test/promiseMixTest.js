/**
 * Test for promiseMix.
 * Runs with mocha.
 */
'use strict'

describe('promise-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
