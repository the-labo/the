/**
 * Test for socketMix.
 * Runs with mocha.
 */
'use strict'

describe('socket-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
