'use strict'
/**
 * Test for socketMix.
 * Runs with mocha.
 */
describe('socket-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
