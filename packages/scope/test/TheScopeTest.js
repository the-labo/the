/**
 * Test for TheScope.
 * Runs with mocha.
 */
'use strict'

const TheScope = require('../lib/TheScope')
const {ok, equal} = require('assert')

describe('the-scope', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(TheScope)
  })
})

/* global describe, before, after, it */
