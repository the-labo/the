'use strict'

/**
 * Test for TheScope.
 * Runs with mocha.
 */
const {
  strict: { ok },
} = require('assert')
const TheScope = require('../lib/TheScope')

describe('the-scope', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(TheScope)
  })
})

/* global describe, before, after, it */
