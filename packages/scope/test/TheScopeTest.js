'use strict'
/**
 * Test for TheScope.
 * Runs with mocha.
 */
const { ok } = require('assert').strict
const TheScope = require('../lib/TheScope')

describe('the-scope', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(TheScope)
  })
})

/* global describe, before, after, it */
