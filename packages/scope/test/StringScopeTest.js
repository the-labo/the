/**
 * Test for StringScope.
 * Runs with mocha.
 */
'use strict'

describe('string-scope', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
