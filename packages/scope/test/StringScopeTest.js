'use strict'
/**
 * Test for StringScope.
 * Runs with mocha.
 */
describe('string-scope', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
