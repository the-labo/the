/**
 * Test for Scope.
 * Runs with mocha.
 */
'use strict'

const Scope = require('../lib/scopes/Scope')
const { ok, equal } = require('assert')

describe('scope', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
