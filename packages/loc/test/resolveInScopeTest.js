/**
 * Test for resolveInScope.
 * Runs with mocha.
 */
'use strict'

describe('resolve-in-scope', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
