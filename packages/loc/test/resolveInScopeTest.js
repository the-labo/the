'use strict'
/**
 * Test for resolveInScope.
 * Runs with mocha.
 */
describe('resolve-in-scope', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
