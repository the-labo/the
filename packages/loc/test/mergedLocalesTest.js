'use strict'
/**
 * Test for mergedLocales.
 * Runs with mocha.
 */
describe('merged-locales', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
