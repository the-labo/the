/**
 * Test for mergedLocales.
 * Runs with mocha.
 */
'use strict'

describe('merged-locales', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
