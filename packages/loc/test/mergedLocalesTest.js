/**
 * Test for mergedLocales.
 * Runs with mocha.
 */
'use strict'

const mergedLocales = require('../lib/helpers/mergedLocales')
const { ok, equal } = require('assert')

describe('merged-locales', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
