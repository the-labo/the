/**
 * Test for toCompoundLocale.
 * Runs with mocha.
 */
'use strict'

describe('to-compound-locale', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
