'use strict'
/**
 * Test for toCompoundLocale.
 * Runs with mocha.
 */
describe('to-compound-locale', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
