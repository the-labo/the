'use strict'

/**
 * Test for listTypes.
 * Runs with mocha.
 */
const listTypes = require('../lib/listTypes')

describe('list-types', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    listTypes({})
  })
})

/* global describe, before, after, it */
