/**
 * Test for listTypes.
 * Runs with mocha.
 */
'use strict'

const listTypes = require('../lib/listTypes')

describe('list-types', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    listTypes({})
  })
})

/* global describe, before, after, it */
