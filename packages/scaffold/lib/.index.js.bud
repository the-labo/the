/**
 * Bud file for index.js
 */
'use strict'

const pkg = require('../package')

module.exports = {
  data: {
    pkg,
  },
  force: true,
  mode: '444',
}

if (!module.parent) {
  require('coz').render(__filename)
}
