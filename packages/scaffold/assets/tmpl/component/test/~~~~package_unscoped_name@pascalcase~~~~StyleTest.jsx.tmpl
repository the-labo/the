/**
 * Test for ~~~~package_unscoped_name@pascalcase~~~~Style.
 * Runs with mocha.
 */
'use strict'

const ~~~~package_unscoped_name@pascalcase~~~~Style = require('../shim/~~~~package_unscoped_name@pascalcase~~~~Style').default
const React = require('react')
const { ok } = require('assert')

describe('~~~~package_unscoped_name~~~~-style', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    const element = React.createElement(~~~~package_unscoped_name@pascalcase~~~~Style)
    ok(element)
  })
})

/* global describe, before, after, it */
