/**
 * Test for ~~~~package_unscoped_name@pascalcase~~~~.
 * Runs with mocha.
 */
'use strict'

const { ok } = require('assert').strict
const React = require('react')
const ~~~~package_unscoped_name@pascalcase~~~~ = require('../shim/~~~~package_unscoped_name@pascalcase~~~~').default

describe('~~~~package_unscoped_name~~~~', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(React.createElement(~~~~package_unscoped_name@pascalcase~~~~))
  })
})

/* global describe, before, after, it */
