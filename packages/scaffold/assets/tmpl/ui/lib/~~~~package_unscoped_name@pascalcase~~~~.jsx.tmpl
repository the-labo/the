/**
 * ~~~~package_description~~~~
 * @memberof module:~~~~package_name~~~~
 * @class ~~~~package_unscoped_name@pascalcase~~~~
 * @extends React.Component
 */
'use strict'

import c from 'classnames'
import React from 'react'
import { eventHandlersFor, htmlAttributesFor } from '@the-/util-ui'

/** @lends ~~~~package_unscoped_name@pascalcase~~~~ */
class ~~~~package_unscoped_name@pascalcase~~~~ extends React.Component {
  render() {
    const { props } = this
    const { children, className } = props
    return (
      <div
        {...htmlAttributesFor(props, { except: ['className'] })}
        {...eventHandlersFor(props, { except: [] })}
        className={c('~~~~package_unscoped_name~~~~', className)}
      >
        {children}
      </div>
    )
  }
}

~~~~package_unscoped_name@pascalcase~~~~.propTypes = {}

~~~~package_unscoped_name@pascalcase~~~~.defaultProps = {}

~~~~package_unscoped_name@pascalcase~~~~.displayName = '~~~~package_unscoped_name@pascalcase~~~~'

export default ~~~~package_unscoped_name@pascalcase~~~~
