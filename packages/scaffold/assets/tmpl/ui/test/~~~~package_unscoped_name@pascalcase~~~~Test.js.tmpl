'use strict'
/**
 * Test for ~~~~package_unscoped_name@pascalcase~~~~.
 * Runs with mocha.
 */
const {
  strict: { ok },
} = require('assert')
const React = require('react')
const { default: ~~~~package_unscoped_name@pascalcase~~~~ } = require('../shim/~~~~package_unscoped_name@pascalcase~~~~')

describe('~~~~package_unscoped_name~~~~', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(React.createElement(~~~~package_unscoped_name@pascalcase~~~~))
  })
})

/* global describe, before, after, it */
