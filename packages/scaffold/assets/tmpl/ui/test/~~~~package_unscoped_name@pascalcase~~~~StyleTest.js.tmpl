'use strict'

/**
 * Test for ~~~~package_unscoped_name@pascalcase~~~~Style.
 * Runs with mocha.
 */
const {
  strict: { ok },
} = require('assert')
const React = require('react')
const { default: ~~~~package_unscoped_name@pascalcase~~~~Style } = require('../shim/~~~~package_unscoped_name@pascalcase~~~~Style')

describe('~~~~package_unscoped_name~~~~-style', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(React.createElement(~~~~package_unscoped_name@pascalcase~~~~Style))
  })
})

/* global describe, before, after, it */
