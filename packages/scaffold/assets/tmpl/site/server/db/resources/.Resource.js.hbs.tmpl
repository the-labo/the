/**
 * @class {{name}}
 * @memberof {{memberof}}
 * @augments TheResource
 */
'use strict'

const {
  TheResource,
  DataTypes: { STRING, ENTITY },
} = require('the-resource-base')

/** @lends {{memberof}}.{{name}} */
function {{name}} ({ define }) {
  const {{shortName}} = define({
    name: {
      description: 'Name of resource',
      type: STRING,
      unique: true,
      required: true,
      trim: true,
      minLength: 2
    }
  }, {
    entityClass: (ResourceEntity)  =>
    /*
     * @memberof {{memberof}}.{{name}}
     * @inner
     * @class {{name}}Entity
     */
    class {{name}}Entity extends ResourceEntity {
    }
  })

  Object.seal({{shortName}})

  return {{shortName}}
}

module.exports = {{name}}
