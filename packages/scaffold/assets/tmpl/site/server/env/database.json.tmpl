{
  "default": {
    "DATABASE": "~~~~package_name@cramcase~~~~_dev2",
    "DIALECT": "rdb/mysql",
    "HOST": "0.0.0.0",
    "PASSWORD": "~~~~package_name@cramcase~~~~_dev",
    "PORT": "#{Local.MYSQL_CONTAINER_PORT}",
    "ROOT_PASSWORD": "root",
    "ROOT_USERNAME": "root",
    "USERNAME": "~~~~package_name@cramcase~~~~_dev"
  },
  "production": {
    "DATABASE": "#{Local.DB_SCHEMA}",
    "DIALECT": "rdb/mysql",
    "HOST": "#{Local.DB_HOST}",
    "PASSWORD": "#{Local.DB_PASSWORD}",
    "PORT": "#{Local.DB_PORT}",
    "ROOT_PASSWORD": "#{Local.DB_ROOT_PASSWORD}",
    "ROOT_USERNAME": "#{Local.DB_ROOT_USER}",
    "USERNAME": "#{Local.DB_USER}"
  },
  "test": {
    "DIALECT": "sqlite",
    "STORAGE": "var/storage/drone-scope-here.db"
  }
}