'use strict'

const { scopes } = require('@the-/templates')

module.exports = scopes({
  dirname: __dirname,
  memberof: 'module:~~~~package_name~~~~/client',
})

if (!module.parent) {
  require('coz').render(__filename)
}
