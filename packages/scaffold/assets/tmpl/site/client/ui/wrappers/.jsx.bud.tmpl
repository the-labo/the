/**
 * Bud file for wrappers
 */
'use strict'

const { Wrap } = require('the-templates')

module.exports = [
].map((name) => Wrap({
  name,
}))

if (!module.parent) {
  require('coz').render(__filename)
}
