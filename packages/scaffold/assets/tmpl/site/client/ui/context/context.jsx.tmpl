/**
 * Context object
 * @memberof module:~~~~package_name~~~~/client.context
 * @namespace context
 */
'use strict'

import { TheContext } from '@the-/context'

const context = new TheContext({})

export default context
