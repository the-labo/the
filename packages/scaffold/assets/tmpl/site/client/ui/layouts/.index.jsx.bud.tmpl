'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  annotations: {
    description: 'Layout Components',
    memberof: 'module:~~~~package_name~~~~/client.ui',
    namespace: 'layouts',
  },
  dirname: __dirname,
  ext: '.jsx',
})

if (!module.parent) {
  require('coz').render(__filename)
}
