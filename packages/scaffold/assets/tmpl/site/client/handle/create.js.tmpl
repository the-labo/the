/**
 * Create a new handle
 * @memberof module:~~~~package_name~~~~/client.handle
 * @function create
 * @returns {TheHandle} Handle instance
 */
'use strict'

import { TheHandle } from '@the-/handle/shim'
import { SceneMapping } from '../mappings'

/** @lends module:~~~~package_name~~~~/client.handle.create */
export default function create() {
  return new TheHandle({
    scenes: SceneMapping,
  })
}
