/**
 * Test for store.
 * Runs with mocha.
 */
'use strict'

const { ok } = require('assert').strict
const store = require('../shim/store')

describe('store', () => {
  before(() => {})

  after(() => {})

  it('Store test', () => {
    ok(store)
  })
})

/* global describe, before, after, it */
