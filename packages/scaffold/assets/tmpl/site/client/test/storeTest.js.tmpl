/**
 * Test for store.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const store = require('../shim/store')

describe('store', () => {
  before(() => {})

  after(() => {})

  it('Store test', () => {
    ok(store)
  })
})

/* global describe, before, after, it */
