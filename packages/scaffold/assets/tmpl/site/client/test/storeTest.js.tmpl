/**
 * Test for store.
 * Runs with mocha.
 */
'use strict'

const store = require('../store')
const {ok, equal} = require('assert')

describe('store', () => {
  before(() => {
  })

  after(() => {
  })

  it('Store test', () => {
    ok(store)
  })
})

/* global describe, before, after, it */
