/**
 * Test for client.
 * Runs with mocha.
 */
'use strict'

const create = require('../client/create')
const {ok, equal} = require('assert')

describe('client', () => {
  before(() => {
  })

  after(() => {
  })

  it('Create test', () => {
    ok(create)
  })
})

/* global describe, before, after, it */
