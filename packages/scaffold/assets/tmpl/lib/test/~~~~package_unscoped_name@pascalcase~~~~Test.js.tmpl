'use strict'
/**
 * Test for ~~~~package_unscoped_name@pascalcase~~~~.
 * Runs with mocha.
 */
const {
  strict: { ok },
} = require('assert')
const ~~~~package_unscoped_name@pascalcase~~~~ = require('../lib/~~~~package_unscoped_name@pascalcase~~~~')

describe('~~~~package_unscoped_name~~~~', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(~~~~package_unscoped_name@pascalcase~~~~)
  })
})

/* global describe, before, after, it */
