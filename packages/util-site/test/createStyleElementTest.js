/**
 * Test for createStyleElement.
 * Runs with mocha.
 */
'use strict'

const createStyleElement = require('../lib/createStyleElement')
const { ok, equal } = require('assert')

describe('create-style-element', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
