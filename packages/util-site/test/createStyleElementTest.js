/**
 * Test for createStyleElement.
 * Runs with mocha.
 */
'use strict'

describe('create-style-element', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
