/**
 * Test for hasMoreFor.
 * Runs with mocha.
 */
'use strict'

const hasMoreFor = require('../lib/hasMoreFor')
const { ok, equal } = require('assert')

describe('has-more-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
