'use strict'
/**
 * Test for queryFromSearch.
 * Runs with mocha.
 */
describe('query-from-search', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
