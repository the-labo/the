/**
 * Test for scrollToElement.
 * Runs with mocha.
 */
'use strict'

describe('scroll-to-element', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
