'use strict'
/**
 * Test for scrollToElement.
 * Runs with mocha.
 */
describe('scroll-to-element', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
