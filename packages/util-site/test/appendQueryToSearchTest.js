/**
 * Test for appendQueryToSearch.
 * Runs with mocha.
 */
'use strict'

describe('append-query-to-search', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
