'use strict'
/**
 * Test for appendQueryToSearch.
 * Runs with mocha.
 */
describe('append-query-to-search', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
