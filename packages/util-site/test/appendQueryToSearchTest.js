/**
 * Test for appendQueryToSearch.
 * Runs with mocha.
 */
'use strict'

const appendQueryToSearch = require('../lib/appendQueryToSearch')
const { ok, equal } = require('assert')

describe('append-query-to-search', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
