/**
 * Test for countSuffix.
 * Runs with mocha.
 */
'use strict'

describe('count-suffix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
