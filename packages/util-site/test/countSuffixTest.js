'use strict'
/**
 * Test for countSuffix.
 * Runs with mocha.
 */
describe('count-suffix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
