/**
 * Test for countSuffix.
 * Runs with mocha.
 */
'use strict'

const countSuffix = require('../lib/countSuffix')
const { ok, equal } = require('assert')

describe('count-suffix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
