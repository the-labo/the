/**
 * Test for omitLongString.
 * Runs with mocha.
 */
'use strict'

describe('omit-long-string', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
