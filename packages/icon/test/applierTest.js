/**
 * Test for applier.
 * Runs with mocha.
 */
'use strict'

const applier = require('../lib/helpers/applier')

const { ok, equal, deepEqual } = require('assert').strict

describe('applier', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
