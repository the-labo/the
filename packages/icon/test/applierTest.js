/**
 * Test for applier.
 * Runs with mocha.
 */
'use strict'

describe('applier', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
