/**
 * Test for createFormData.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const createFormData = require('../lib/createFormData')

describe('create-form-data', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(createFormData)
  })
})

/* global describe, before, after, it */
