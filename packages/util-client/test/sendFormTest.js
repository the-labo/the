/**
 * Test for sendForm.
 * Runs with mocha.
 */
'use strict'

describe('send-form', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
