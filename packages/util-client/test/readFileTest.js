'use strict'
/**
 * Test for readFile.
 * Runs with mocha.
 */
describe('read-file', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
