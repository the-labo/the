/**
 * Bud file for test cases.
 */
'use strict'

const { Test } = require('@the-/templates/presets/component')

module.exports = Test(__dirname)

if (!module.parent) {
  require('coz').render(__filename)
}
