'use strict'
/**
 * Test for TheForbiddenError.
 * Runs with mocha.
 */
describe('the-forbidden-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
