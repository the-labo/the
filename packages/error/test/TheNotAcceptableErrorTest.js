/**
 * Test for TheNotAcceptableError.
 * Runs with mocha.
 */
'use strict'

describe('the-not-acceptable-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
