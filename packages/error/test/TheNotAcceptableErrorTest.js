'use strict'
/**
 * Test for TheNotAcceptableError.
 * Runs with mocha.
 */
describe('the-not-acceptable-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
