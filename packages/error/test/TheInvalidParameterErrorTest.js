/**
 * Test for TheInvalidParameterError.
 * Runs with mocha.
 */
'use strict'

describe('the-invalid-parameter-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
