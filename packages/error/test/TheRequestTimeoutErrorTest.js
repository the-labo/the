'use strict'
/**
 * Test for TheRequestTimeoutError.
 * Runs with mocha.
 */
describe('the-request-timeout-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
