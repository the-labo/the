/**
 * Test for TheRequestTimeoutError.
 * Runs with mocha.
 */
'use strict'

describe('the-request-timeout-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
