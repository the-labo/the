/**
 * Test for TheNotFoundError.
 * Runs with mocha.
 */
'use strict'

describe('the-not-found-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
