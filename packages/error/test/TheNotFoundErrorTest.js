'use strict'
/**
 * Test for TheNotFoundError.
 * Runs with mocha.
 */
describe('the-not-found-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
