'use strict'
/**
 * Test for TheExpiredError.
 * Runs with mocha.
 */
describe('the-expired-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
