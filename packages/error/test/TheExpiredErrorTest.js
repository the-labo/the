/**
 * Test for TheExpiredError.
 * Runs with mocha.
 */
'use strict'

describe('the-expired-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
