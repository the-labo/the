/**
 * Test for TheUnauthorizedError.
 * Runs with mocha.
 */
'use strict'

describe('the-unauthorized-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
