'use strict'
/**
 * Test for TheUnauthorizedError.
 * Runs with mocha.
 */
describe('the-unauthorized-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
