/**
 * Test for TheGoneError.
 * Runs with mocha.
 */
'use strict'

describe('the-gone-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
