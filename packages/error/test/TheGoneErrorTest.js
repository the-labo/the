'use strict'
/**
 * Test for TheGoneError.
 * Runs with mocha.
 */
describe('the-gone-error', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
