/**
 * Test for defineModelIndexes.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const defineModelIndexes = require('../lib/modeling/defineModelIndexes')

describe('define-model-indexes', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(defineModelIndexes)
  })
})

/* global describe, before, after, it */
