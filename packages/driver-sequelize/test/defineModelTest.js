/**
 * Test for defineModel.
 * Runs with mocha.
 */
'use strict'

describe('define-model', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
