/**
 * Test for MetaColumnNamesReversed.
 * Runs with mocha.
 */
'use strict'

describe('meta-column-names-reversed', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
