'use strict'
/**
 * Test for MetaColumnNamesReversed.
 * Runs with mocha.
 */
describe('meta-column-names-reversed', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
