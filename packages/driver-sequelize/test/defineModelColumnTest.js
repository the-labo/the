/**
 * Test for defineModelColumn.
 * Runs with mocha.
 */
'use strict'

describe('define-model-column', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
