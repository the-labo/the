'use strict'
/**
 * Test for defineModelColumn.
 * Runs with mocha.
 */
describe('define-model-column', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
