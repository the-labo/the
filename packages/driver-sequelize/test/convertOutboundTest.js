/**
 * Test for convertOutbound.
 * Runs with mocha.
 */
'use strict'

describe('convert-outbound', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
