'use strict'
/**
 * Test for parseSort.
 * Runs with mocha.
 */
describe('parse-sort', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
