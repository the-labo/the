/**
 * Test for parseSort.
 * Runs with mocha.
 */
'use strict'

describe('parse-sort', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
