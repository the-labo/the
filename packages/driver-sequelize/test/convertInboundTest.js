'use strict'
/**
 * Test for convertInbound.
 * Runs with mocha.
 */
describe('convert-inbound', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
