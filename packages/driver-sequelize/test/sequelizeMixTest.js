'use strict'
/**
 * @file Test for sequelizeMix.
 * Runs with mocha.
 */
describe('sequelize-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
