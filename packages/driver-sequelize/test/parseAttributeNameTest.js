'use strict'
/**
 * @file Test for parseAttributeName.
 * Runs with mocha.
 */
describe('parse-attribute-name', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
