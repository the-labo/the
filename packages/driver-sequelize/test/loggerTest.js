'use strict'
/**
 * @file Test for logger.
 * Runs with mocha.
 */
describe('logger', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
