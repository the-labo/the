'use strict'
/**
 * Test for prepareModel.
 * Runs with mocha.
 */
describe('prepare-model', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
