/**
 * Test for serializer.
 * Runs with mocha.
 */
'use strict'

describe('serializer', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
