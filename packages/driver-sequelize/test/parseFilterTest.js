/**
 * Test for parseFilter.
 * Runs with mocha.
 */
'use strict'

describe('parse-filter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
