'use strict'
/**
 * @file Test for parseFilter.
 * Runs with mocha.
 */
describe('parse-filter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
