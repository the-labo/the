/**
 * Test for MetaColumnNames.
 * Runs with mocha.
 */
'use strict'

describe('meta-column-names', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
