'use strict'
/**
 * Test for MetaColumnNames.
 * Runs with mocha.
 */
describe('meta-column-names', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
