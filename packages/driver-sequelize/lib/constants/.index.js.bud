'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  dirname: __dirname,
  name: 'constant variables',
})

if (!module.parent) {
  require('coz').render(__filename)
}
