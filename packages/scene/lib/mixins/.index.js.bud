'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  description: 'Scene mixin functions',
  dirname: __dirname,
  name: 'mixins',
})

if (!module.parent) {
  require('coz').render(__filename)
}
