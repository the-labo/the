/**
 * Test for subMix.
 * Runs with mocha.
 */
'use strict'

describe('sub-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
