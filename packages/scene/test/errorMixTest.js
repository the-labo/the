/**
 * Test for errorMix.
 * Runs with mocha.
 */
'use strict'

describe('error-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
