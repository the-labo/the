/**
 * Test for contextEntryFor.
 * Runs with mocha.
 */
'use strict'

describe('context-entry-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
