'use strict'
/**
 * Test for contextEntryFor.
 * Runs with mocha.
 */
describe('context-entry-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
