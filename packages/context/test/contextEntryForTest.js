/**
 * Test for contextEntryFor.
 * Runs with mocha.
 */
'use strict'

const contextEntryFor = require('../lib/helpers/contextEntryFor')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('context-entry-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
