'use strict'
/**
 * Test for contextRootFor.
 * Runs with mocha.
 */
describe('context-root-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
