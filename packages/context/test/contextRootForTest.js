/**
 * Test for contextRootFor.
 * Runs with mocha.
 */
'use strict'

const contextRootFor = require('../lib/helpers/contextRootFor')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('context-root-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
