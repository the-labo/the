/**
 * Test for buildShim.
 * Runs with mocha.
 */
'use strict'

describe('build-shim', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
