'use strict'
/**
 * Test for buildShim.
 * Runs with mocha.
 */
describe('build-shim', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
