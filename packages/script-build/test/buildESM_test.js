/**
 * Test case for buildESM.
 * Runs with mocha.
 */
'use strict'

describe('build-es-m', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Build es m', async () => {})
})

/* global describe, before, after, it */
