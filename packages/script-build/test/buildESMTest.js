/**
 * Test for buildESM.
 * Runs with mocha.
 */
'use strict'

describe('build-es-m', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
