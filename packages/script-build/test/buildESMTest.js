'use strict'
/**
 * Test for buildESM.
 * Runs with mocha.
 */
describe('build-es-m', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
