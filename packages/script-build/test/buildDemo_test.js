/**
 * Test case for buildDemo.
 * Runs with mocha.
 */
'use strict'

describe('build-demo', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Build demo', async () => {})
})

/* global describe, before, after, it */
