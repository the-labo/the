'use strict'
/**
 * Test for buildDemo.
 * Runs with mocha.
 */
describe('build-demo', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
