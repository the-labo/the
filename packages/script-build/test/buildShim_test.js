/**
 * Test case for buildShim.
 * Runs with mocha.
 */
'use strict'

describe('build-shim', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Build shim', async () => {})
})

/* global describe, before, after, it */
