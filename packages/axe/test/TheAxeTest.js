/**
 * Test for TheAxe.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const TheAxe = require('../lib/TheAxe')

describe('the-axe', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(TheAxe)
  })
})

/* global describe, before, after, it */
