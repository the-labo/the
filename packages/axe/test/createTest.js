/**
 * Test for create.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const create = require('../lib/create')

describe('create', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(create)
  })
})

/* global describe, before, after, it */
