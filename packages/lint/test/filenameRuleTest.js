/**
 * Test for filenameRule.
 * Runs with mocha.
 */
'use strict'

describe('filename-rule', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {})
})

/* global describe, before, after, it */
