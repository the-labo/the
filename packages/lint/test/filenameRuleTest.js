'use strict'
/**
 * Test for filenameRule.
 * Runs with mocha.
 */
describe('filename-rule', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {})
})

/* global describe, before, after, it */
