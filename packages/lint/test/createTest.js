'use strict'
/**
 * Test for create.
 * Runs with mocha.
 */
describe('create', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {})
})

/* global describe, before, after, it */
