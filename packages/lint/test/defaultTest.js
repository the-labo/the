'use strict'
/**
 * Test for default.
 * Runs with mocha.
 */
describe('default', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {})
})

/* global describe, before, after, it */
