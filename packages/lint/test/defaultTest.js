/**
 * Test for default.
 * Runs with mocha.
 */
'use strict'

describe('default', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {})
})

/* global describe, before, after, it */
