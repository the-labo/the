'use strict'
/**
 * Test for LintCache.
 * Runs with mocha.
 */
describe('lint-cache', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
