/**
 * Test for LintCache.
 * Runs with mocha.
 */
'use strict'

describe('lint-cache', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
