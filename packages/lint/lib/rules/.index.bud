'use strict'

const {dir} = require('the-templates')

module.exports = [
  true,
].map((cjs) => ({
  ...dir({
    cjs,
    dirname: __dirname,
    name: 'rules',
    description: 'Build in Lint Rules',
  })
}))

if (!module.parent) {
  require('coz').render(__filename)
}