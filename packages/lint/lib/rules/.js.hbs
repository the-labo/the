/**
 * Create "{{name}}" lint
 * @function {{name}}
 * @param {Object} config - Lint config
 * @returns {function} Lint function
 */
'use strict'

/** @lends {{name}} */
function {{name}} (config) {
  const {
    ...rest
  } = config
  if (Object.keys(rest).length > 0) {
    console.warn(`[{{name}}] Unknown options`, Object.keys(rest))
  }
return async function {{name}}Check ({
                                  filename,
                                  content,
                                  report,
                                 }) {

  }
}

module.exports = {{name}}
