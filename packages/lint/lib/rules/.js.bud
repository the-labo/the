'use strict'

const names = [
  'usageRule',
]
module.exports = names.map((name) => ({
  force: false,
  mode: '666',
  path: `${name}.js`,
  data: { name }
}))

if (!module.parent) {
  require('coz').render(__filename)
}
