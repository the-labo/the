'use strict'

const names = ['packageRule']
module.exports = names.map((name) => ({
  data: { name },
  force: false,
  mode: '666',
  path: `${name}.js`,
}))

if (!module.parent) {
  require('coz').render(__filename)
}
