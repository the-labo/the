/**
 * Test for helpers.
 * Runs with mocha.
 */
'use strict'

const helpers = require('../lib/helpers')
const { ok, equal } = require('assert')

describe('helpers', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
