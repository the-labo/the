/**
 * Test for toHandleSceneClass.
 * Runs with mocha.
 */
'use strict'

describe('to-handle-scene-class', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
