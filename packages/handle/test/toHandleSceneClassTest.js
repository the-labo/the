/**
 * Test for toHandleSceneClass.
 * Runs with mocha.
 */
'use strict'

const toHandleSceneClass = require('../lib/toHandleSceneClass')
const { ok, equal } = require('assert')

describe('to-handle-scene-class', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
