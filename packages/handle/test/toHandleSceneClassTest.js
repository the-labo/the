'use strict'
/**
 * Test for toHandleSceneClass.
 * Runs with mocha.
 */
describe('to-handle-scene-class', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
