/**
 * Test for sendSendgrid.
 * Runs with mocha.
 */
'use strict'

describe('send-sendgrid', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
