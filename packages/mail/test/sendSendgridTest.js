'use strict'
/**
 * Test for sendSendgrid.
 * Runs with mocha.
 */
describe('send-sendgrid', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
