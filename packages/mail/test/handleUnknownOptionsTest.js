/**
 * Test for handleUnknownOptions.
 * Runs with mocha.
 */
'use strict'

describe('handle-unknown-options', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
