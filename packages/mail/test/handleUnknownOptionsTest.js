'use strict'
/**
 * Test for handleUnknownOptions.
 * Runs with mocha.
 */
describe('handle-unknown-options', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
