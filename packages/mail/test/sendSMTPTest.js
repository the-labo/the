'use strict'
/**
 * Test for sendSMTP.
 * Runs with mocha.
 */
describe('send-s-m-t-p', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
