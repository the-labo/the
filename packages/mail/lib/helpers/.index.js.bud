'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  description: 'Helper functions',
  dirname: __dirname,
  name: 'helpers',
})

if (!module.parent) {
  require('coz').render(__filename)
}
