'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  description: 'Sending functions',
  dirname: __dirname,
  name: 'sending',
})

if (!module.parent) {
  require('coz').render(__filename)
}
