'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  dirname: __dirname,
  cjs: true,
  name: 'sending',
  description: 'Sending functions'
})

if (!module.parent) {
  require('coz').render(__filename)
}
