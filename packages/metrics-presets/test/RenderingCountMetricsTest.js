'use strict'
/**
 * Test for RenderingCountMetrics.
 * Runs with mocha.
 */
describe('rendering-count-metrics', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
