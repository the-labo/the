/**
 * Test for RenderingCountMetrics.
 * Runs with mocha.
 */
'use strict'

describe('rendering-count-metrics', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
