/**
 * Test for ActionTypes.
 * Runs with mocha.
 */
'use strict'

const ActionTypes = require('../lib/constants/ActionTypes')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('action-types', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
