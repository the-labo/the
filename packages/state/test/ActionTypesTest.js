/**
 * Test for ActionTypes.
 * Runs with mocha.
 */
'use strict'

describe('action-types', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
