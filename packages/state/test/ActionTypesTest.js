'use strict'
/**
 * Test for ActionTypes.
 * Runs with mocha.
 */
describe('action-types', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
