/**
 * Test for reduxHelper.
 * Runs with mocha.
 */
'use strict'

const reduxHelper = require('../lib/helpers/reduxHelper')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('redux-helper', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
