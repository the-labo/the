/**
 * Test for ScopedState.
 * Runs with mocha.
 */
'use strict'

const ScopedState = require('../lib/state/ScopedState')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('scoped-state', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
