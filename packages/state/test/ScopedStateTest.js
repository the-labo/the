/**
 * Test for ScopedState.
 * Runs with mocha.
 */
'use strict'

describe('scoped-state', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
