/**
 * Test for RootState.
 * Runs with mocha.
 */
'use strict'

const RootState = require('../lib/state/RootState')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('root-state', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
