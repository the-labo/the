/**
 * Test for RootState.
 * Runs with mocha.
 */
'use strict'

describe('root-state', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
