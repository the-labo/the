'use strict'
/**
 * Test for RootState.
 * Runs with mocha.
 */
describe('root-state', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
