/**
 * Test for scopeMix.
 * Runs with mocha.
 */
'use strict'

describe('scope-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
