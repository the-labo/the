'use strict'
/**
 * Test for getUserAgent.
 * Runs with mocha.
 */
describe('get-user-agent', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
