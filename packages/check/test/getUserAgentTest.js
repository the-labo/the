/**
 * Test for getUserAgent.
 * Runs with mocha.
 */
'use strict'

describe('get-user-agent', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
