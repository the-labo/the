/**
 * Test for canTouch.
 * Runs with mocha.
 */
'use strict'

describe('can-touch', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
