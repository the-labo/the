'use strict'
/**
 * Test for canTouch.
 * Runs with mocha.
 */
describe('can-touch', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
