/**
 * {{name}} mixin
 * @function {{name}}
 * @param {function} Class - Class to mix
 * @returns {function} Mixed class
 */
'use strict'

const asClassMixin = require('./helpers/asClassMixin')

/** @lends {{name}} */
const {{name}} = asClassMixin((Class) => {
  class {{pascalcase name}} extends Class {

  }

  return {{pascalcase name}}
})

module.exports = {{name}}
