/**
 * Test for withId.
 * Runs with mocha.
 */
'use strict'

const withId = require('../lib/withId')
const { ok, equal } = require('assert')

describe('with-id', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
