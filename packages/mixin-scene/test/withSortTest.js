/**
 * Test for withSort.
 * Runs with mocha.
 */
'use strict'

const withSort = require('../lib/withSort')
const { ok, equal } = require('assert')

describe('with-sort', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
