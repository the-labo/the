/**
 * Test for withSort.
 * Runs with mocha.
 */
'use strict'

describe('with-sort', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
