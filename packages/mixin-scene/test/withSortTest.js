'use strict'
/**
 * Test for withSort.
 * Runs with mocha.
 */
describe('with-sort', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
