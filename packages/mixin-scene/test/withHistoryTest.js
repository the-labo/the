/**
 * Test for withHistory.
 * Runs with mocha.
 */
'use strict'

describe('with-history', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
