/**
 * Test for withReady.
 * Runs with mocha.
 */
'use strict'

const withReady = require('../lib/withReady')
const { ok, equal } = require('assert')

describe('with-ready', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
