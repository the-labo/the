/**
 * Test for withReady.
 * Runs with mocha.
 */
'use strict'

describe('with-ready', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
