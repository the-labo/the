'use strict'
/**
 * Test for withReady.
 * Runs with mocha.
 */
describe('with-ready', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
