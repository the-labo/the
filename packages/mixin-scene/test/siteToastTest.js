'use strict'
/**
 * Test for siteToast.
 * Runs with mocha.
 */
describe('site-toast', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
