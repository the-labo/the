/**
 * Test for siteToast.
 * Runs with mocha.
 */
'use strict'

const siteToast = require('../lib/siteToast')
const { ok, equal } = require('assert')

describe('site-toast', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
