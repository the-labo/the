/**
 * Test for siteToast.
 * Runs with mocha.
 */
'use strict'

describe('site-toast', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
