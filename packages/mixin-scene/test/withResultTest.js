'use strict'
/**
 * Test for withResult.
 * Runs with mocha.
 */
describe('with-result', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
