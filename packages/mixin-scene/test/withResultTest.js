/**
 * Test for withResult.
 * Runs with mocha.
 */
'use strict'

describe('with-result', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
