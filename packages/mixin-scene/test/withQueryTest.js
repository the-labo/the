/**
 * Test for withQuery.
 * Runs with mocha.
 */
'use strict'

describe('with-query', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
