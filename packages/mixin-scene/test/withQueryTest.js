'use strict'
/**
 * Test for withQuery.
 * Runs with mocha.
 */
describe('with-query', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
