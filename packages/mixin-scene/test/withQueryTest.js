/**
 * Test for withQuery.
 * Runs with mocha.
 */
'use strict'

const withQuery = require('../lib/withQuery')
const { ok, equal } = require('assert')

describe('with-query', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
