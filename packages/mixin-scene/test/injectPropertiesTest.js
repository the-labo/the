'use strict'
/**
 * Test for injectProperties.
 * Runs with mocha.
 */
describe('inject-properties', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
