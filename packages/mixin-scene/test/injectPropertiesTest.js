/**
 * Test for injectProperties.
 * Runs with mocha.
 */
'use strict'

describe('inject-properties', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
