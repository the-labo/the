/**
 * Test for asClassMixin.
 * Runs with mocha.
 */
'use strict'

describe('as-class-mixin', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
