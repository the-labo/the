/**
 * Test for withFailure.
 * Runs with mocha.
 */
'use strict'

const withFailure = require('../lib/withFailure')
const { ok, equal } = require('assert')

describe('with-failure', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
