/**
 * Test for withPage.
 * Runs with mocha.
 */
'use strict'

const withPage = require('../lib/withPage')
const { ok, equal } = require('assert')

describe('with-page', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
