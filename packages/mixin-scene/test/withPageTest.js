'use strict'
/**
 * Test for withPage.
 * Runs with mocha.
 */
describe('with-page', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
