/**
 * @file Test for default.
 * Runs with mocha.
 */
'use strict'
const {
  strict: { ok },
} = require('assert')
const lib = require('../lib/default')

describe('default', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(lib)
  })
})

/* global describe, before, after, it */
