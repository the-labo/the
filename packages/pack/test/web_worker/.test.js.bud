/**
 * @file Bud file for test cases.
 */
'use strict'

const { test } = require('@the-/templates')

module.exports = test({
  cjs: true,
  dest: __dirname,
  src: [`${__dirname}/../../lib/web_worker/*.js`],
})

if (!module.parent) {
  require('coz').render(__filename)
}
