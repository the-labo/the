/**
 * Test for applyTrackConstraints.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const applyTrackConstraints = require('../lib/helpers/applyTrackConstraints')

describe('apply-track-constraints', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(applyTrackConstraints)
  })
})

/* global describe, before, after, it */
