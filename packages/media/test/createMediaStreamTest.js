/**
 * Test for createMediaStream.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const createMediaStream = require('../lib/helpers/createMediaStream')

describe('create-media-stream', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(createMediaStream)
  })
})

/* global describe, before, after, it */
