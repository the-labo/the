/**
 * Test for constraintHelper.
 * Runs with mocha.
 */
'use strict'

describe('constraint-helper', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
