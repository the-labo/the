/**
 * Test for constraintHelper.
 * Runs with mocha.
 */
'use strict'

const constraintHelper = require('../lib/constraintHelper')
const { ok, equal } = require('assert')

describe('constraint-helper', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
