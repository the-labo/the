/**
 * Test for helpers.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const helpers = require('../lib/helpers')

describe('helpers', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(helpers)
  })
})

/* global describe, before, after, it */
