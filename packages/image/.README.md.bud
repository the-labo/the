/**
 * Bud file for README.md
 */
'use strict'

const { Readme } = require('@the-/templates/presets/component')

module.exports = Readme(__dirname)

if (!module.parent) {
  require('coz').render(__filename)
}
