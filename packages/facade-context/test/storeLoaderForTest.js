/**
 * Test for storeLoaderFor.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const storeLoaderFor = require('../lib/storeLoaderFor')

describe('store-loader-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(storeLoaderFor)
  })
})

/* global describe, before, after, it */
