/**
 * Test for VendorColors.
 * Runs with mocha.
 */
'use strict'

describe('vendor-colors', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
