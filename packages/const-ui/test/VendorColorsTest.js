/**
 * Test for VendorColors.
 * Runs with mocha.
 */
'use strict'

const VendorColors = require('../lib/VendorColors')

const { ok, equal, deepEqual } = require('assert').strict

describe('vendor-colors', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
