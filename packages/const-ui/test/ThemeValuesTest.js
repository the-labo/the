/**
 * Test for ThemeValues.
 * Runs with mocha.
 */
'use strict'

const ThemeValues = require('../lib/ThemeValues')

const { ok, equal, deepEqual } = require('assert').strict

describe('theme-values', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
