'use strict'
/**
 * Test for ThemeValues.
 * Runs with mocha.
 */
describe('theme-values', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
