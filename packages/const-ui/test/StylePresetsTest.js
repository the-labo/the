/**
 * Test for StylePresets.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const StylePresets = require('../lib/StylePresets')

describe('style-presets', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(StylePresets)
  })
})

/* global describe, before, after, it */
