'use strict'
/**
 * Test for helpers.
 * Runs with mocha.
 */
describe('helpers', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
