/**
 * Test for entitiesAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('entities-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
