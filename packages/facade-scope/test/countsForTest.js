/**
 * Test for countsFor.
 * Runs with mocha.
 */
'use strict'

describe('counts-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
