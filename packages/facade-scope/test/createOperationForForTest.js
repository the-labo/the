/**
 * Test for createOperationForFor.
 * Runs with mocha.
 */
'use strict'

const createOperationForFor = require('../lib/operations/createOperationForFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('create-operation-for-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
