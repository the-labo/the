/**
 * Test for filterAccessFor.
 * Runs with mocha.
 */
'use strict'

const filterAccessFor = require('../lib/filterAccessFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('filter-access-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
