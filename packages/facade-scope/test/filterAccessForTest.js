/**
 * Test for filterAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('filter-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
