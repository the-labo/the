/**
 * Test for listOperationFor.
 * Runs with mocha.
 */
'use strict'

describe('list-operation-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
