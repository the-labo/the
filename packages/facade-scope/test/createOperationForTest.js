/**
 * Test for createOperationFor.
 * Runs with mocha.
 */
'use strict'

describe('create-operation-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
