/**
 * Test for pageAccessFor.
 * Runs with mocha.
 */
'use strict'

const pageAccessFor = require('../lib/pageAccessFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('page-access-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
