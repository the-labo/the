/**
 * Test for countsAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('counts-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
