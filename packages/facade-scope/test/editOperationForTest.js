/**
 * Test for editOperationFor.
 * Runs with mocha.
 */
'use strict'

describe('edit-operation-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
