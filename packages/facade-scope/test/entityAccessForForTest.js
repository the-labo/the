/**
 * Test for entityAccessForFor.
 * Runs with mocha.
 */
'use strict'

const entityAccessForFor = require('../lib/entityAccessForFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('entity-access-for-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
