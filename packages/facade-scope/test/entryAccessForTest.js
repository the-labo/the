/**
 * Test for entryAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('entry-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
