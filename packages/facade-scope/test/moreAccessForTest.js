/**
 * Test for moreAccessFor.
 * Runs with mocha.
 */
'use strict'

const moreAccessFor = require('../lib/moreAccessFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('more-access-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
