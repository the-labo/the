/**
 * Test for moreAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('more-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
