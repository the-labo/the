/**
 * Test for busyAccessFor.
 * Runs with mocha.
 */
'use strict'

const busyAccessFor = require('../lib/busyAccessFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('busy-access-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
