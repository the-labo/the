/**
 * Test for readyAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('ready-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
