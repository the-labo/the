/**
 * Test for sortAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('sort-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
