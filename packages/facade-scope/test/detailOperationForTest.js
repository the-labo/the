/**
 * Test for detailOperationFor.
 * Runs with mocha.
 */
'use strict'

describe('detail-operation-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
