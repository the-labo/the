/**
 * Test for entityAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('entity-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
