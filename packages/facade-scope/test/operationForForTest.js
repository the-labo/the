/**
 * Test for operationForFor.
 * Runs with mocha.
 */
'use strict'

const operationForFor = require('../lib/operations/operationForFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('operation-for-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
