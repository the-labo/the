/**
 * Test for idAccessFor.
 * Runs with mocha.
 */
'use strict'

const idAccessFor = require('../lib/idAccessFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('id-access-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
