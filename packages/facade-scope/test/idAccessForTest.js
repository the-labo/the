/**
 * Test for idAccessFor.
 * Runs with mocha.
 */
'use strict'

describe('id-access-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
