/**
 * Test for sortFor.
 * Runs with mocha.
 */
'use strict'

describe('sort-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
