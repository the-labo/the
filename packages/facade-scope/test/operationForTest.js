/**
 * Test for operationFor.
 * Runs with mocha.
 */
'use strict'

describe('operation-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
