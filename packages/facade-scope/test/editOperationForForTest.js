/**
 * Test for editOperationForFor.
 * Runs with mocha.
 */
'use strict'

const editOperationForFor = require('../lib/operations/editOperationForFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('edit-operation-for-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
