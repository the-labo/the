/**
 * Test for destroyOperationForFor.
 * Runs with mocha.
 */
'use strict'

const destroyOperationForFor = require('../lib/operations/destroyOperationForFor')

const { ok, equal, deepEqual } = require('assert').strict

describe('destroy-operation-for-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
