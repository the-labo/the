'use strict'

const path = require('path')

module.exports = ['operations/operation'].map((name) => ({
  data: {
    name: path.basename(name),
  },
  force: false,
  mkdirp: true,
  mode: '644',
  path: `${name}For.js`,
}))

if (!module.parent) {
  require('coz').render(__filename)
}
