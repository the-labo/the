/**
 * Test for setMix.
 * Runs with mocha.
 */
'use strict'

const setMix = require('../lib/mixins/setMix')
const { ok, equal } = require('assert')

describe('set-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
