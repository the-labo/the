/**
 * Test for startOfMix.
 * Runs with mocha.
 */
'use strict'

describe('start-of-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
