/**
 * Test for startOfMix.
 * Runs with mocha.
 */
'use strict'

const startOfMix = require('../lib/mixins/startOfMix')
const { ok, equal } = require('assert')

describe('start-of-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
