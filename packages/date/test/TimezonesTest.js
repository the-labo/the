/**
 * Test for Timezones.
 * Runs with mocha.
 */
'use strict'

describe('timezones', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
