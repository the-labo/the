/**
 * Test for Timezones.
 * Runs with mocha.
 */
'use strict'

const Timezones = require('../lib/Timezones')
const { ok, equal } = require('assert')

describe('timezones', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
