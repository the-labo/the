'use strict'
/**
 * Test for Timezones.
 * Runs with mocha.
 */
describe('timezones', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
