'use strict'
/**
 * Test for subtractMix.
 * Runs with mocha.
 */
describe('subtract-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
