/**
 * Test for addMix.
 * Runs with mocha.
 */
'use strict'

describe('add-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
