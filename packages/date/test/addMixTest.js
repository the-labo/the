/**
 * Test for addMix.
 * Runs with mocha.
 */
'use strict'

const addMix = require('../lib/mixins/addMix')
const { ok, equal } = require('assert')

describe('add-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
