/**
 * Test for Durations.
 * Runs with mocha.
 */
'use strict'

describe('durations', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
