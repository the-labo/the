/**
 * Test for Durations.
 * Runs with mocha.
 */
'use strict'

const Durations = require('../lib/Durations')
const { ok, equal } = require('assert')

describe('durations', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
