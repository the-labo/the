/**
 * Test for getMix.
 * Runs with mocha.
 */
'use strict'

const getMix = require('../lib/mixins/getMix')
const { ok, equal } = require('assert')

describe('get-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
