/**
 * Test for getMix.
 * Runs with mocha.
 */
'use strict'

describe('get-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
