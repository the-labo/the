'use strict'
/**
 * Test for momentMix.
 * Runs with mocha.
 */
describe('moment-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
