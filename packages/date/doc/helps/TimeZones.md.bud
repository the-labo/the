'use strict'

const { Timezones } = require('../../lib')

module.exports = {
  data: {
    Timezones,
  },
  force: true,
  mode: '644',
}

if (!module.parent) {
  require('coz').render(__filename)
}
