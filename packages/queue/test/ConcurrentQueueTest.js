/**
 * @file Test for ConcurrentQueue.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const ConcurrentQueue = require('../lib/ConcurrentQueue')

describe('concurrent-queue', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(ConcurrentQueue)
  })
})

/* global describe, before, after, it */
