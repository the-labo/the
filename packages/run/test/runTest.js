/**
 * Test for run.
 * Runs with mocha.
 */
'use strict'

const run = require('../lib/run')
const { ok, equal } = require('assert')

describe('run', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
