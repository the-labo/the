'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  annotations: {
    desc: 'Mix functions',
    namespace: 'mixins',
    memberOf: 'module:@the-/setting'
  },
  dirname: __dirname,
})

if (!module.parent) {
  require('coz').render(__filename)
}
