/**
 * Test for lockMix.
 * Runs with mocha.
 */
'use strict'

const lockMix = require('../lib/mixins/lockMix')
const { ok, equal } = require('assert')

describe('lock-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
