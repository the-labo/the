/**
 * Test for acquire.
 * Runs with mocha.
 */
'use strict'

const acquire = require('../lib/acquire')
const { ok, equal } = require('assert')

describe('acquire', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
