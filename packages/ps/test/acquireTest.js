/**
 * Test for acquire.
 * Runs with mocha.
 */
'use strict'

describe('acquire', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
