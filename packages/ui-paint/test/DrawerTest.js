/**
 * Test for Drawer.
 * Runs with mocha.
 */
'use strict'

import '@babel/polyfill'
import Drawer from '../lib/helpers/Drawer'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from '@the-/script-test'

describe('drawer', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
  })
})

/* global describe, before, after, it */
