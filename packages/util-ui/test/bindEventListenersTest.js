/**
 * Test for bindEventListeners.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const bindEventListeners = require('../lib/bindEventListeners')

describe('bind-event-listeners', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(bindEventListeners)
  })
})

/* global describe, before, after, it */
