/**
 * Test for stopTouchScrolling.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const stopTouchScrolling = require('../lib/stopTouchScrolling')

describe('stop-touch-scrolling', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(stopTouchScrolling)
  })
})

/* global describe, before, after, it */
