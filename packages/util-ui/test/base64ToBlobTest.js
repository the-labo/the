/**
 * Test for base64ToBlob.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const base64ToBlob = require('../lib/base64ToBlob')

describe('base64-to-blob', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(base64ToBlob)
  })
})

/* global describe, before, after, it */
