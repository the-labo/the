/**
 * @file Test for observeResize.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const observeResize = require('../lib/observeResize')

describe('observe-resize', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(observeResize)
  })
})

/* global describe, before, after, it */
