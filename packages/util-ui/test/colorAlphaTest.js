'use strict'
/**
 * Test for colorAlpha.
 * Runs with mocha.
 */
describe('color-alpha', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
