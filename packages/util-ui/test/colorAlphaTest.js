/**
 * Test for colorAlpha.
 * Runs with mocha.
 */
'use strict'

describe('color-alpha', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
