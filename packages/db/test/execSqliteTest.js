'use strict'
/**
 * Test for execSqlite.
 * Runs with mocha.
 */
describe('exec-sqlite', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
