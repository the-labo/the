/**
 * Test for execSqlite.
 * Runs with mocha.
 */
'use strict'

describe('exec-sqlite', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
