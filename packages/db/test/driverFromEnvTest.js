/**
 * Test for driverFromEnv.
 * Runs with mocha.
 */
'use strict'

describe('driver-from-env', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
