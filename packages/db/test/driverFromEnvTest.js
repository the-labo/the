'use strict'
/**
 * Test for driverFromEnv.
 * Runs with mocha.
 */
describe('driver-from-env', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
