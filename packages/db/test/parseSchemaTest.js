/**
 * Test for parseSchema.
 * Runs with mocha.
 */
'use strict'

describe('parse-schema', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
