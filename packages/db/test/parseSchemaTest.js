'use strict'
/**
 * Test for parseSchema.
 * Runs with mocha.
 */
describe('parse-schema', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
