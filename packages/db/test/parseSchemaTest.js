/**
 * Test for parseSchema.
 * Runs with mocha.
 */
'use strict'

const parseSchema = require('../lib/helpers/parseSchema')
const { ok, equal, deepEqual } = require('assert').strict

describe('parse-schema', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
