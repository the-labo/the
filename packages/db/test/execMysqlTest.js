/**
 * Test for execMysql.
 * Runs with mocha.
 */
'use strict'

const execMysql = require('../lib/helpers/execMysql')
const { ok, equal } = require('assert')

describe('exec-mysql', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
