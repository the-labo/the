'use strict'
/**
 * Test for cascadeMix.
 * Runs with mocha.
 */
describe('cascade-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
