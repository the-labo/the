/**
 * Test for cascadeMix.
 * Runs with mocha.
 */
'use strict'

const cascadeMix = require('../lib/mixins/cascadeMix')
const { ok, equal } = require('assert')

describe('cascade-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
