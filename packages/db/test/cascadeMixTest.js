/**
 * Test for cascadeMix.
 * Runs with mocha.
 */
'use strict'

describe('cascade-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
