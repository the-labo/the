/**
 * Test for setupMongo.
 * Runs with mocha.
 */
'use strict'

describe('setup-mongo', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
