'use strict'
/**
 * Test for setupMongo.
 * Runs with mocha.
 */
describe('setup-mongo', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
