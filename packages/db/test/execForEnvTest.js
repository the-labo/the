/**
 * Test for execForEnv.
 * Runs with mocha.
 */
'use strict'

const execForEnv = require('../lib/execForEnv')
const { ok, equal } = require('assert')

describe('exec-for-env', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
