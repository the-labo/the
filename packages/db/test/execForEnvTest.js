/**
 * Test for execForEnv.
 * Runs with mocha.
 */
'use strict'

describe('exec-for-env', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
