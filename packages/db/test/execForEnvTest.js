'use strict'
/**
 * Test for execForEnv.
 * Runs with mocha.
 */
describe('exec-for-env', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
