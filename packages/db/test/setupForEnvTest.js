'use strict'
/**
 * Test for setupForEnv.
 * Runs with mocha.
 */
describe('setup-for-env', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
