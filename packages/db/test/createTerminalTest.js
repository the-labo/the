'use strict'
/**
 * Test for createTerminal.
 * Runs with mocha.
 */
describe('create-terminal', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
