/**
 * Test for createTerminal.
 * Runs with mocha.
 */
'use strict'

const createTerminal = require('../lib/helpers/createTerminal')
const { ok, equal } = require('assert')

describe('create-terminal', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
