'use strict'
/**
 * Test for setupPostgres.
 * Runs with mocha.
 */
describe('setup-postgres', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
