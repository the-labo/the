/**
 * Test for setupPostgres.
 * Runs with mocha.
 */
'use strict'

const setupPostgres = require('../lib/setup/setupPostgres')
const { ok, equal } = require('assert')

describe('setup-postgres', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
