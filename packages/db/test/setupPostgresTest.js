/**
 * Test for setupPostgres.
 * Runs with mocha.
 */
'use strict'

describe('setup-postgres', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
