/**
 * Test for setupSqlite.
 * Runs with mocha.
 */
'use strict'

describe('setup-sqlite', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
