/**
 * Test for setupSqlite.
 * Runs with mocha.
 */
'use strict'

const setupSqlite = require('../lib/setup/setupSqlite')
const {ok, equal} = require('assert')

describe('setup-sqlite', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
