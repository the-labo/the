/**
 * Test for migrateMix.
 * Runs with mocha.
 */
'use strict'

const migrateMix = require('../lib/mixins/migrateMix')
const { ok, equal } = require('assert')

describe('migrate-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
