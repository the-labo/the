'use strict'
/**
 * Test for migrateMix.
 * Runs with mocha.
 */
describe('migrate-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
