/**
 * Test for migrateMix.
 * Runs with mocha.
 */
'use strict'

describe('migrate-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
