/**
 * Test for resourceMix.
 * Runs with mocha.
 */
'use strict'

const resourceMix = require('../lib/mixins/resourceMix')
const { ok, equal } = require('assert')

describe('resource-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
