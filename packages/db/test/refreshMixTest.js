/**
 * Test for refreshMix.
 * Runs with mocha.
 */
'use strict'

describe('refresh-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
