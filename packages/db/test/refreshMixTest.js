'use strict'
/**
 * Test for refreshMix.
 * Runs with mocha.
 */
describe('refresh-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
