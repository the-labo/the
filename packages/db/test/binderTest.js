/**
 * Test for binder.
 * Runs with mocha.
 */
'use strict'

const binder = require('../lib/helpers/binder')
const { ok, equal } = require('assert')

describe('binder', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
