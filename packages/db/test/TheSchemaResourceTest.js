/**
 * Test for TheSchemaResource.
 * Runs with mocha.
 */
'use strict'

describe('the-schema-resource', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
