/**
 * Test for cliMix.
 * Runs with mocha.
 */
'use strict'

const cliMix = require('../lib/mixins/cliMix')
const { ok, equal } = require('assert')

describe('cli-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
