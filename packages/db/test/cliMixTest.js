/**
 * Test for cliMix.
 * Runs with mocha.
 */
'use strict'

describe('cli-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
