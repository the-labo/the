/**
 * Test for TheLogResource.
 * Runs with mocha.
 */
'use strict'

describe('the-log-resource', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
