'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  description: 'Buildin resources',
  dirname: __dirname,
  name: 'resources',
})

if (!module.parent) {
  require('coz').render(__filename)
}
