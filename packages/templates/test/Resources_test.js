/**
 * Test case for resources.
 * Runs with mocha.
 */
'use strict'

describe('resources', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Resources', async () => {})
})

/* global describe, before, after, it */
