/**
 * Test case for pcssVars.
 * Runs with mocha.
 */
'use strict'

describe('pcss-vars', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Pcss vars', async () => {})
})

/* global describe, before, after, it */
