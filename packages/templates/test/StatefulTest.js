/**
 * Test for Stateful.
 * Runs with mocha.
 */
'use strict'

describe('stateful', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
