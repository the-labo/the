/**
 * Test for eslintrc.
 * Runs with mocha.
 */
'use strict'

describe('eslintrc', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
