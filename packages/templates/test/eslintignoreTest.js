/**
 * Test for eslintignore.
 * Runs with mocha.
 */
'use strict'

describe('eslintignore', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
