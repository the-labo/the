/**
 * Test for Dialogs.
 * Runs with mocha.
 */
'use strict'

describe('dialogs', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
