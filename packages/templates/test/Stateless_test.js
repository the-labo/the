/**
 * Test case for stateless.
 * Runs with mocha.
 */
'use strict'

describe('stateless', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Stateless', async () => {})
})

/* global describe, before, after, it */
