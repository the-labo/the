/**
 * Test for prettierrc.
 * Runs with mocha.
 */
'use strict'

describe('prettierrc', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
