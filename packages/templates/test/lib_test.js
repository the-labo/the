/**
 * Test case for lib.
 * Runs with mocha.
 */
'use strict'

describe('lib', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Lib', async () => {})
})

/* global describe, before, after, it */
