/**
 * Test for errorPage.
 * Runs with mocha.
 */
'use strict'

describe('error-page', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
