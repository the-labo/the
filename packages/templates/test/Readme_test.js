/**
 * Test case for readme.
 * Runs with mocha.
 */
'use strict'

describe('readme', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Readme', async () => {})
})

/* global describe, before, after, it */
