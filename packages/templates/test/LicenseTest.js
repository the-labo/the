/**
 * Test for License.
 * Runs with mocha.
 */
'use strict'

describe('license', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
