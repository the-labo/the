/**
 * Test case for bin.
 * Runs with mocha.
 */
'use strict'

describe('bin', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Bin', async () => {})
})

/* global describe, before, after, it */
