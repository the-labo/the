'use strict'
/**
 * Test for pcssVars.
 * Runs with mocha.
 */
describe('pcss-vars', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
