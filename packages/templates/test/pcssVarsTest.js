/**
 * Test for pcssVars.
 * Runs with mocha.
 */
'use strict'

describe('pcss-vars', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
