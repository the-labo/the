/**
 * Test for Resources.
 * Runs with mocha.
 */
'use strict'

describe('resources', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
