'use strict'
/**
 * Test for Resources.
 * Runs with mocha.
 */
describe('resources', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
