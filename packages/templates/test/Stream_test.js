'use strict'
/**
 * Test case for stream.
 * Runs with mocha.
 */
describe('stream', function () {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Stream', async () => {})
})

/* global describe, before, after, it */
