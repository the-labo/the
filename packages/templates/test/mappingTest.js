/**
 * Test for mapping.
 * Runs with mocha.
 */
'use strict'

describe('mapping', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
