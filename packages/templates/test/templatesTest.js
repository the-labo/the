/**
 * Test for templates.
 * Runs with mocha.
 */
'use strict'

describe('templates', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
