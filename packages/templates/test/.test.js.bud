/**
 * Bud file for test cases.
 */
'use strict'

const { test } = require('../lib')

module.exports = test({
  cjs: true,
  dest: __dirname,
  src: [`${__dirname}/../lib/**/*.js`],
})

if (!module.parent) {
  require('coz').render(__filename)
}
