/**
 * Test case for templates.
 * Runs with mocha.
 */
'use strict'

describe('templates', function() {
  this.timeout(3000)

  before(async () => {})

  after(async () => {})

  it('Templates', async () => {})
})

/* global describe, before, after, it */
