/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

const Stream = require('./Stream')
const {compose, } = require('the-stream-mixins')

const {{name}}Base = compose(

)(Stream)

/** @lends {{name}} */
class {{name}} extends {{name}}Base {
}

module.exports = {{name}}
