/**
 * {{name}}
 * @memberof {{memberof}}
 * @function {{name}}
 */
'use strict'

const Stream = require('./Stream')

/** @lends {{memberof}}.{{name}} */
const {{name}} = ({ params }) => {
  return {
    async consume(received) {

    },
    async *provide() {

    },
  }
}

module.exports = {{name}}
