<!-- // Code generated by coz. DO NOT EDIT. -->
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>{{title}} - {{pkg.name}}</title>
  <meta class="the-head-viewport" name="viewport" content="width=device-width,initial-scale=1.0"/>
  <meta name="theme-color" content="{{color}}"/>
  <link rel="icon" href="{{icon}}"/>
  <style>
    html {
      font-family: "ヒラギノ角ゴ ProN W3", "Hiragino Kaku Gothic ProN", メイリオ, Meiryo, sans-serif;
      background: white;
    }

    body {
      color: #555;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: stretch;
      justify-content: space-between;
      box-sizing: border-box;
      margin: 0;
    }

    .container {
      max-width: 724px;
      margin: 0 auto;
    }

    header {
      display: block;
      padding: 4px 16px;
      border-bottom: 2px solid {{color}};
    }

    header h1 {
      font-weight: normal;
      margin: 0;
      font-size: 1em;
      line-height: 1em;
    }

    header h1 a {
      text-decoration: none;
      color: inherit;
    }

    main {
      padding: 16px;
      height: 100%;
      display: block;
      flex-grow: 1;
    }

    article {
      max-width: 960px;
      margin: 16px auto;
      padding: 0;
      border-radius: 8px;
    }

    h1 {
      margin: 8px 0 16px;
    }

    hr {
      margin: 24px 0;
      border: 0 solid #DDD;
      border-top-width: 1px;
    }

    a {
      color: {{color}};
    }

    .btn {
      color: {{color}};
      text-decoration: none;
      padding: 4px 8px;
      display: inline-block;
      border-radius: 4px;
      border: 1px solid;
    }

    .row {
      display: flex;
      justify-content: space-between;
      padding: 4px 0;
    }

    footer {
      padding: 8px;
      color: #AAA;
      font-size: smaller;
      display: block;
      width: 100%;
      text-align: right;
      box-sizing: border-box;
    }

    {{{style}}}
  </style>
</head>
<body>
<header>
  <div class="container">
    <h1><a href="/">{{pkg.name}}</a></h1>
  </div>
</header>
<main>
  <div class="container">
    <article>
      <h1>{{{title}}}</h1>
      {{#each texts}}
        <p>{{{this}}}</p>
      {{/each}}
      <hr>
      <div class="row">
        <span></span>
        <a href="/" class="btn">Return to Top</a>
      </div>
    </article>
    <div class="container">
</main>
<footer>
</footer>
</body>
</html>