/**
 * {{name}}
 * @memberof {{memberof}}
 * @function {{name}}
 */
'use strict'

/** @lends {{memberof}}.{{name}} */
const {{name}} = ({ }) => {
  const {{camelcase name}} = {

  }

  Object.freeze({{camelcase name}})

  return {{camelcase name}}
}

export default {{name}}
