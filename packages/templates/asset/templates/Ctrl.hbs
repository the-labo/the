/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

import Ctrl from './Ctrl'
import {compose, } from '@the-/mixin-controller'

const {{name}}Base = compose(

)(Ctrl)

/** @lends {{name}} */
class {{name}} extends {{name}}Base {
}

export default {{name}}
