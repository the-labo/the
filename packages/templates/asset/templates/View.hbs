/**
 * {{pascalcase name}} component
 * @memberof {{memberOf}}
 * @function {{pascalcase name}}
 */
'use strict'

import React from 'react'
import { TheMeta, TheView, TheCycle } from '@the-/ui'
import styles from './{{pascalcase name}}.pcss'
import { Stateful } from '../../context'

const stateful = useCallback(Stateful(
  (state) => ({
  }),
  ({}) => ({
    onMount: () => {},
    onUnmount: () => {},
  })
)

/** @lends {{memberOf}}.{{pascalcase name}} */
const {{pascalcase name}} = React.memo(() => {
  return stateful(
    ({
      l,
      onMount,
      onUnmount,
    }) => {
      const title = null
      return (
        <TheCycle {...{ onMount, onUnmount, }}>
          <TheMeta title={title}>
            <TheView className={styles.self}>
              <TheView.Header icon={null}
                              text={title}
              />
              <TheView.Body>
                This is {{pascalcase name}}!
              </TheView.Body>
            </TheView>
          </TheMeta>
        </TheCycle>
      )
    }
  )
}

export default {{pascalcase name}}
