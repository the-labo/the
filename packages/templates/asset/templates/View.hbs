/**
 * {{pascalcase name}} component
 * @class {{pascalcase name}}
 */
'use strict'

import React from 'react'
import { TheMeta, TheView, TheCycle } from 'the-components'
import styles from './{{pascalcase name}}.pcss'
import context from '../../context'

/** @lends class {{pascalcase name}} */
class {{pascalcase name}} extends React.PureComponent {
  #stateful = context.stateful(
    (state) => ({
    }),
    ({}) => ({
      onMount: () => {},
      onUnmount: () => {},
    })
  )

  render () {
    return this.#stateful(
      ({
        l,
        onMount,
        onUnmount,
      }) => {
        const title = null
        return (
          <TheCycle {...{ onMount, onUnmount, }}>
            <TheMeta title={title}>
              <TheView className={styles.self}>
                <TheView.Header icon={null}
                                text={title}
                />
                <TheView.Body>
                  This is {{pascalcase name}}!
                </TheView.Body>
              </TheView>
            </TheMeta>
          </TheCycle>
        )
      }
    )
  }
}

export default {{pascalcase name}}
