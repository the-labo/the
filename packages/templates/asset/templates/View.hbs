'use strict'

import React, { useEffect } from 'react'
import { TheMeta, TheView } from '@the-/ui'
import styles from './{{pascalcase name}}.pcss'
import { Stateful } from '../../context'

const stateful = Stateful(
  (state) => ({
  }),
  ({}) => ({
    onMount: () => {},
    onUnmount: () => {},
  })
)

/**
 * {{pascalcase name}} component
 * @memberof {{memberOf}}
 * @function {{pascalcase name}}
 */
const {{pascalcase name}} = React.memo(() => {
  useEffect(() => {
    handle.onMount()
    return () => handle.onUnmount()
  }, [])
  return stateful(
    ({
      l,
      onMount,
      onUnmount,
    }) => {
      const title = null
      return (
        <TheMeta title={title}>
          <TheView className={styles.self}>
            <TheView.Header icon={null}
                            text={title}
              />
            <TheView.Body>
              This is {{pascalcase name}}!
            </TheView.Body>
          </TheView>
        </TheMeta>
      )
    }
  )
})

export default {{pascalcase name}}
