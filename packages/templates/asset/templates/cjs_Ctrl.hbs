/**
 * {{name}}
 * @memberof {{memberof}}
 * @class {{name}}
 */
'use strict'

const Ctrl = require('./Ctrl')
const {compose, } = require('@the-/mixin-controller')

const {{name}}Base = compose(

)(Ctrl)

/** @lends {{memberof}}.{{name}} */
class {{name}} extends {{name}}Base {
}

module.exports = {{name}}
