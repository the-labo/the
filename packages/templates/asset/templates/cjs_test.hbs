/**
 * Test for {{name}}.
 * Runs with mocha.
 */
'use strict'

const {{varName}} = require('{{{relative}}}')
const { ok, equal, deepEqual } = require('assert').strict

describe('{{spinalcase name}}', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
