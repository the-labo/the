/**
 * @file Test for {{name}}.
 * Runs with mocha.
 */
'use strict'

const {{varName}} = require('{{{relative}}}'){{#if useDefault}}.default{{/if}}
{{#each deps}}const {{@key}} = require('{{{this}}}'){{/each}}
const { ok, equal, deepEqual } = require('assert').strict

describe('{{spinalcase name}}', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', async () => {
    ok({{name}})
    {{{content}}}
  })
})

/* global describe, before, after, it */
