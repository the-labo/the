/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

import Stream from './Stream'
import {compose, } from 'the-controller-mixins'

const {{name}}Base = compose(

)(Stream)

/** @lends {{name}} */
class {{name}} extends {{name}}Base {
}

export default {{name}}
