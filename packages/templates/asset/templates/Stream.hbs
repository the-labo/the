'use strict'

const Stream = require('./Stream')

/**
 * {{name}}
 * @memberof {{memberof}}
 * @function {{name}}
 */
const {{name}} = ({ params }) => {
  return {
    async consume(received) {

    },
    async *provide() {

    },
  }
}

module.exports = {{name}}
