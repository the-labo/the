/**
 * {{name}}
 * @memberof {{memberof}}
 * @class {{name}}
 */
'use strict'

import Stream from './Stream'
import {compose, } from '@the-/mixin-stream'

const {{name}}Base = compose(

)(Stream)

/** @lends {{memberof}}.{{name}} */
class {{name}} extends {{name}}Base {
}

export default {{name}}
