/**
 * Test for installFiles.
 * Runs with mocha.
 */
'use strict'

describe('install-files', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
