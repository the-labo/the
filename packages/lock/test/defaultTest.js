/**
 * Test for default.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const default_ = require('../lib/default')

describe('default', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(default_)
  })
})

/* global describe, before, after, it */
