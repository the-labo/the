'use strict'
/**
 * Test for TheS3.
 * Runs with mocha.
 */
const { ok } = require('assert').strict
const TheS3 = require('../lib/TheS3')

describe('the-s3', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {
    ok(TheS3)
  })
})

/* global describe, before, after, it */
