'use strict'

const t = require('@babel/types')
console.log(JSON.stringify(t.VISITOR_KEYS, null, 4))

module.exports = {
  force: true,
  mode: '444',
  data: {
    types: Object.keys(t.VISITOR_KEYS),
  }
}

if (!module.parent) {
  require('coz').render(__filename)
}
