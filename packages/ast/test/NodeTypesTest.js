/**
 * Test for NodeTypes.
 * Runs with mocha.
 */
'use strict'

describe('node-types', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
