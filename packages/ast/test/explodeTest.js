/**
 * Test for explode.
 * Runs with mocha.
 */
'use strict'

const explode = require('../lib/walk/explode')
const { ok, equal, deepEqual } = require('assert').strict

describe('explode', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
