/**
 * Test for full.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const full = require('../lib/full')

describe('full', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(full)
  })
})

/* global describe, before, after, it */
