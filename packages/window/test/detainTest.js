/**
 * Test for detain.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const detain = require('../lib/detain')

describe('detain', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(detain)
  })
})

/* global describe, before, after, it */
