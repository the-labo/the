'use strict'
/**
 * Test for writeOnceMix.
 * Runs with mocha.
 */
describe('write-once-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
