/**
 * Test for writeOnceMix.
 * Runs with mocha.
 */
'use strict'

const writeOnceMix = require('../lib/mixins/writeOnceMix')
const { ok, equal } = require('assert')

describe('write-once-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
