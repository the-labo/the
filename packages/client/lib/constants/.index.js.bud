'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  dirname: __dirname,
  annotations: {
    namespace: 'constants',
    description: 'Constant variables',
  },
})

if (!module.parent) {
  require('coz').render(__filename)
}
