'use strict'

const IOEvents = require('the-server/lib/constants/IOEvents')

module.exports = {
  force: true,
  mode: '444',
  data: {
    values: IOEvents,
  }
}
if (!module.parent) {
  require('coz').render(__filename)
}