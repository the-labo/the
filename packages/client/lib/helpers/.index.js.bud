'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  annotations:{
    namespace: 'helpers',
    description: 'Client helper functions',
  },
  dirname: __dirname,
})

if (!module.parent) {
  require('coz').render(__filename)
}
