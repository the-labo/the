'use strict'

const {dir} = require('@the-/templates')

module.exports = dir({
  dirname: __dirname,
  name: 'helpers',
  description: 'Client helper functions',
  cjs: true,
})

if (!module.parent) {
  require('coz').render(__filename)
}
