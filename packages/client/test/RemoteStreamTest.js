/**
 * Test for RemoteStream.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const RemoteStream = require('../lib/helpers/RemoteStream')

describe('remote-stream', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(RemoteStream)
  })
})

/* global describe, before, after, it */
