/**
 * @file Test for InfoAccess.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const InfoAccess = require('../lib/helpers/InfoAccess')

describe('info-access', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(InfoAccess)
  })
})

/* global describe, before, after, it */
