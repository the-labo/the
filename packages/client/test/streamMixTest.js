/**
 * Test for streamMix.
 * Runs with mocha.
 */
'use strict'

describe('stream-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
