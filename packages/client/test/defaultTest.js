'use strict'
/**
 * @file Test for default.
 * Runs with mocha.
 */
describe('default', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
