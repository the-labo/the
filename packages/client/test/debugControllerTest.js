'use strict'
/**
 * @file Test for debugController.
 * Runs with mocha.
 */
describe('debug-controller', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
