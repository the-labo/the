/**
 * Test for debugController.
 * Runs with mocha.
 */
'use strict'

describe('debug-controller', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
