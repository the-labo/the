/**
 * Test for infoMix.
 * Runs with mocha.
 */
'use strict'

const infoMix = require('../lib/mixins/infoMix')
const { ok, equal } = require('assert')

describe('info-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
