/**
 * Test for PingSender.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const PingSender = require('../lib/helpers/PingSender')

describe('ping-sender', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(PingSender)
  })
})

/* global describe, before, after, it */
