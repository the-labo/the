/**
 * Test for pingPongMix.
 * Runs with mocha.
 */
'use strict'

describe('ping-pong-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
