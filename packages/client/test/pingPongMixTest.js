/**
 * Test for pingPongMix.
 * Runs with mocha.
 */
'use strict'

const pingPongMix = require('../lib/mixins/pingPongMix')
const { ok, equal } = require('assert')

describe('ping-pong-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
