'use strict'
/**
 * @file Test for parseClientUrl.
 * Runs with mocha.
 */
describe('parse-client-url', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
