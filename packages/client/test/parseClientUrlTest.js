/**
 * Test for parseClientUrl.
 * Runs with mocha.
 */
'use strict'

describe('parse-client-url', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
