'use strict'
/**
 * Test for debugStream.
 * Runs with mocha.
 */
describe('debug-stream', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
