'use strict'
/**
 * Test for IOEvents.
 * Runs with mocha.
 */
describe('i-o-events', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
