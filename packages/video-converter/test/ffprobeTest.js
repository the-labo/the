'use strict'
/**
 * Test for ffprobe.
 * Runs with mocha.
 */
describe('ffprobe', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
