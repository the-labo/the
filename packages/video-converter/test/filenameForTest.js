'use strict'
/**
 * Test for filenameFor.
 * Runs with mocha.
 */
describe('filename-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
