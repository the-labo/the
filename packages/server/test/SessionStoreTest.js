'use strict'
/**
 * Test for SessionStore.
 * Runs with mocha.
 */
describe('session-store', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
