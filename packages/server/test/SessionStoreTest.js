/**
 * Test for SessionStore.
 * Runs with mocha.
 */
'use strict'

describe('session-store', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
