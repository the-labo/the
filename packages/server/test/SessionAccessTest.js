/**
 * Test for SessionAccess.
 * Runs with mocha.
 */
'use strict'

const SessionAccess = require('../lib/helpers/SessionAccess')

const { ok, equal, deepEqual } = require('assert').strict

describe('session-access', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
