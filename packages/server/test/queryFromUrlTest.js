'use strict'
/**
 * Test for queryFromUrl.
 * Runs with mocha.
 */
describe('query-from-url', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
