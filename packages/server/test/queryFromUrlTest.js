/**
 * Test for queryFromUrl.
 * Runs with mocha.
 */
'use strict'

describe('query-from-url', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
