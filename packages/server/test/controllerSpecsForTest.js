'use strict'
/**
 * Test for controllerSpecsFor.
 * Runs with mocha.
 */
describe('controller-specs-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
