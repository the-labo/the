/**
 * Test for controllerSpecsFor.
 * Runs with mocha.
 */
'use strict'

describe('controller-specs-for', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
