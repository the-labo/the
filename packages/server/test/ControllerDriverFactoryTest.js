/**
 * Test for ControllerDriverFactory.
 * Runs with mocha.
 */
'use strict'

describe('controller-driver-factory', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
