'use strict'
/**
 * Test for keepMix.
 * Runs with mocha.
 */
describe('keep-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
