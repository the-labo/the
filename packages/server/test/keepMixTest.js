/**
 * Test for keepMix.
 * Runs with mocha.
 */
'use strict'

const keepMix = require('../lib/mixins/keepMix')
const { ok, equal } = require('assert')

describe('keep-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
