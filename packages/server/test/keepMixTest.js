/**
 * Test for keepMix.
 * Runs with mocha.
 */
'use strict'

describe('keep-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
