'use strict'
/**
 * Test for callbacksProxy.
 * Runs with mocha.
 */
describe('callbacks-proxy', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
