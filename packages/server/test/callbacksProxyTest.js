/**
 * Test for callbacksProxy.
 * Runs with mocha.
 */
'use strict'

describe('callbacks-proxy', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
