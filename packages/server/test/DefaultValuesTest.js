/**
 * Test for DefaultValues.
 * Runs with mocha.
 */
'use strict'

describe('default-values', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
