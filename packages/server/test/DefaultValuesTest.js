/**
 * Test for DefaultValues.
 * Runs with mocha.
 */
'use strict'

const DefaultValues = require('../lib/constants/DefaultValues')
const { ok, equal } = require('assert')

describe('default-values', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
