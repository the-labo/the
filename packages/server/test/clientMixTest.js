'use strict'
/**
 * Test for clientMix.
 * Runs with mocha.
 */
describe('client-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
