/**
 * Test for assertMethods.
 * Runs with mocha.
 */
'use strict'

const assertMethods = require('../lib/assert/assertMethods')
const { ok, equal } = require('assert')

describe('assert-methods', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
