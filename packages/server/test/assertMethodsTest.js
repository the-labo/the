/**
 * Test for assertMethods.
 * Runs with mocha.
 */
'use strict'

describe('assert-methods', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
