'use strict'
/**
 * Test for assertMethods.
 * Runs with mocha.
 */
describe('assert-methods', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
