/**
 * Test for toControllerModuleBind.
 * Runs with mocha.
 */
'use strict'

const toControllerModuleBind = require('../lib/helpers/toControllerModuleBind')
const { ok, equal } = require('assert')

describe('to-controller-module-bind', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
