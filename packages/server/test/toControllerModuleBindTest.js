'use strict'
/**
 * Test for toControllerModuleBind.
 * Runs with mocha.
 */
describe('to-controller-module-bind', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
