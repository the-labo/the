/**
 * Test for toStreamFactory.
 * Runs with mocha.
 */
'use strict'

describe('to-stream-factory', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
