/**
 * Test for ClientAccess.
 * Runs with mocha.
 */
'use strict'

const {
  strict: { ok },
} = require('assert')
const ClientAccess = require('../lib/helpers/ClientAccess')

describe('client-access', () => {
  before(() => {})

  after(() => {})

  it('Do test', async () => {
    ok(ClientAccess)
  })
})

/* global describe, before, after, it */
