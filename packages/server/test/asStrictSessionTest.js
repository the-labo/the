'use strict'
/**
 * Test for asStrictSession.
 * Runs with mocha.
 */
describe('as-strict-session', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
