/**
 * Test for asStrictSession.
 * Runs with mocha.
 */
'use strict'

describe('as-strict-session', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
