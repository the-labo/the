/**
 * Test for ioMix.
 * Runs with mocha.
 */
'use strict'

const ioMix = require('../lib/mixins/ioMix')
const { ok, equal } = require('assert')

describe('io-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
