/**
 * Test for toControllerFactory.
 * Runs with mocha.
 */
'use strict'

describe('to-controller-factory', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
