/**
 * Test for Store.
 * Runs with mocha.
 */
'use strict'

const Store = require('../lib/stores/Store')
const { ok, equal } = require('assert')

describe('store', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
