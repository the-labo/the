/**
 * Test for langDetector.
 * Runs with mocha.
 */
'use strict'

describe('lang-detector', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
