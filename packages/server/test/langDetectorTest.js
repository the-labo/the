/**
 * Test for langDetector.
 * Runs with mocha.
 */
'use strict'

const langDetector = require('../lib/helpers/langDetector')
const { ok, equal } = require('assert')

describe('lang-detector', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
