'use strict'
/**
 * Test for langDetector.
 * Runs with mocha.
 */
describe('lang-detector', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
