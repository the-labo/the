/**
 * Test for ctxInjector.
 * Runs with mocha.
 */
'use strict'

describe('ctx-injector', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
