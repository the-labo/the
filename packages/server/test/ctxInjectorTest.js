'use strict'
/**
 * Test for ctxInjector.
 * Runs with mocha.
 */
describe('ctx-injector', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
