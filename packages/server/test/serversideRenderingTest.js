'use strict'
/**
 * Test for serversideRendering.
 * Runs with mocha.
 */
describe('serverside-rendering', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
