/**
 * Test for serversideRendering.
 * Runs with mocha.
 */
'use strict'

describe('serverside-rendering', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
