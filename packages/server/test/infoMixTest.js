/**
 * Test for infoMix.
 * Runs with mocha.
 */
'use strict'

describe('info-mix', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
