/**
 * Test for ConnectionStore.
 * Runs with mocha.
 */
'use strict'

describe('connection-store', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
