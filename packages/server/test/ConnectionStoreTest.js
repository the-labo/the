/**
 * Test for ConnectionStore.
 * Runs with mocha.
 */
'use strict'

const ConnectionStore = require('../lib/stores/ConnectionStore')
const { ok, equal } = require('assert')

describe('connection-store', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
