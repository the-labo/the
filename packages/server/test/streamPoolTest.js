/**
 * Test for streamPool.
 * Runs with mocha.
 */
'use strict'

describe('stream-pool', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
