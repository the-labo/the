'use strict'
/**
 * Test for streamPool.
 * Runs with mocha.
 */
describe('stream-pool', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
