/**
 * Test for RPCKeeper.
 * Runs with mocha.
 */
'use strict'

describe('rpc-keeper', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
