/**
 * Test for MetricsCounter.
 * Runs with mocha.
 */
'use strict'

const MetricsCounter = require('../lib/helpers/MetricsCounter')

const { ok, equal, deepEqual } = require('assert').strict

describe('metrics-counter', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    
  })
})

/* global describe, before, after, it */
