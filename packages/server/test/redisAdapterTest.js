/**
 * Test for redisAdapter.
 * Runs with mocha.
 */
'use strict'

describe('redis-adapter', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
