/**
 * Test for redisAdapter.
 * Runs with mocha.
 */
'use strict'

const redisAdapter = require('../lib/adpters/redisAdapter')
const { ok, equal } = require('assert')

describe('redis-adapter', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
