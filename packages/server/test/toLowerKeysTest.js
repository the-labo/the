'use strict'
/**
 * Test for toLowerKeys.
 * Runs with mocha.
 */
describe('to-lower-keys', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
