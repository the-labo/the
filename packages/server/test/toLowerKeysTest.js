/**
 * Test for toLowerKeys.
 * Runs with mocha.
 */
'use strict'

describe('to-lower-keys', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
