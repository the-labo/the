'use strict'
/**
 * Test for buildInEndpoints.
 * Runs with mocha.
 */
describe('build-in-endpoints', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
