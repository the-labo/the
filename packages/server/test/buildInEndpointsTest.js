/**
 * Test for buildInEndpoints.
 * Runs with mocha.
 */
'use strict'

describe('build-in-endpoints', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
