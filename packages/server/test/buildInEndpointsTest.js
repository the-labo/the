/**
 * Test for buildInEndpoints.
 * Runs with mocha.
 */
'use strict'

const buildInEndpoints = require('../lib/buildInEndpoints')
const { ok, equal } = require('assert')

describe('build-in-endpoints', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
