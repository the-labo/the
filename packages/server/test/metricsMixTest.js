/**
 * Test for metricsMix.
 * Runs with mocha.
 */
'use strict'

const metricsMix = require('../lib/mixins/metricsMix')
const { ok, equal } = require('assert')

describe('metrics-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
