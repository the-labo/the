/**
 * Test for ControllerPool.
 * Runs with mocha.
 */
'use strict'

describe('controller-pool', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
