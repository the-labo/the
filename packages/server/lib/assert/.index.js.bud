'use strict'

const {dir} = require('the-templates')

module.exports = dir({
  dirname: __dirname,
  cjs: true,
  name: 'assert',
  description: 'Assert functions'
})

if (!module.parent) {
  require('coz').render(__filename)
}