'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  description: 'Adapter functions',
  dirname: __dirname,
  name: 'adapter',
})

if (!module.parent) {
  require('coz').render(__filename)
}
