'use strict'

const { dir } = require('@the-/templates')

module.exports = dir({
  cjs: true,
  description: 'Data stores',
  dirname: __dirname,
  name: 'stores',
})

if (!module.parent) {
  require('coz').render(__filename)
}
