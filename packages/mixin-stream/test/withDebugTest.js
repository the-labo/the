/**
 * Test for withDebug.
 * Runs with mocha.
 */
'use strict'

describe('with-debug', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
