'use strict'
/**
 * Test for withDebug.
 * Runs with mocha.
 */
describe('with-debug', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
