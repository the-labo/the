/**
 * Test for withDebug.
 * Runs with mocha.
 */
'use strict'

const withDebug = require('../lib/withDebug')
const { ok, equal } = require('assert')

describe('with-debug', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
