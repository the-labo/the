/**
 * Test for compose.
 * Runs with mocha.
 */
'use strict'

describe('compose', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
