/**
 * Test for compose.
 * Runs with mocha.
 */
'use strict'

const compose = require('../lib/compose')
const { ok, equal } = require('assert')

describe('compose', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
