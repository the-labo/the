/**
 * Test for sortProperties.
 * Runs with mocha.
 */
'use strict'

describe('sort-properties', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
