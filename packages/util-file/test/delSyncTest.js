/**
 * Test for delSync.
 * Runs with mocha.
 */
'use strict'

describe('del-sync', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
