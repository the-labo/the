/**
 * Test for delSync.
 * Runs with mocha.
 */
'use strict'

const delSync = require('../lib/delSync')
const { ok, equal } = require('assert')

describe('del-sync', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
