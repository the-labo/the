'use strict'
/**
 * Test for delSync.
 * Runs with mocha.
 */
describe('del-sync', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
