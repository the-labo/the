/**
 * Test for mount.
 * Runs with mocha.
 */
'use strict'

const mount = require('../lib/mount')
const { ok, equal } = require('assert')

describe('mount', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
