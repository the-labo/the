/**
 * Test for secure.
 * Runs with mocha.
 */
'use strict'

const secure = require('../lib/secure')
const { ok, equal } = require('assert')

describe('secure', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
