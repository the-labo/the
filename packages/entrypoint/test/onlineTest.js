/**
 * Test for online.
 * Runs with mocha.
 */
'use strict'

const online = require('../lib/online')
const { ok, equal } = require('assert')

describe('online', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
