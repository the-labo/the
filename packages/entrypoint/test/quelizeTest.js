'use strict'
/**
 * Test for quelize.
 * Runs with mocha.
 */
describe('quelize', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
