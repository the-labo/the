/**
 * Test for quelize.
 * Runs with mocha.
 */
'use strict'

const quelize = require('../lib/quelize')
const { ok, equal } = require('assert')

describe('quelize', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
