/**
 * Test for quelize.
 * Runs with mocha.
 */
'use strict'

describe('quelize', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
