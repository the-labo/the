/**
 * Test for singleton.
 * Runs with mocha.
 */
'use strict'

const singleton = require('../lib/singleton')
const { ok, equal } = require('assert')

describe('singleton', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
