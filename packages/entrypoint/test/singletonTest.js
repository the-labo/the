'use strict'
/**
 * Test for singleton.
 * Runs with mocha.
 */
describe('singleton', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
