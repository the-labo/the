/**
 * Test for patch.
 * Runs with mocha.
 */
'use strict'

const patch = require('../lib/patch')
const { ok, equal } = require('assert')

describe('patch', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
