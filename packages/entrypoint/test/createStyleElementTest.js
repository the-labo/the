'use strict'
/**
 * Test for createStyleElement.
 * Runs with mocha.
 */
describe('create-style-element', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
