'use strict'
/**
 * Test for history.
 * Runs with mocha.
 */
describe('history', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
