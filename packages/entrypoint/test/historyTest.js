/**
 * Test for history.
 * Runs with mocha.
 */
'use strict'

const history = require('../lib/history')
const { ok, equal } = require('assert')

describe('history', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
