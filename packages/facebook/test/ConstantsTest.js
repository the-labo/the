'use strict'
/**
 * Test for Constants.
 * Runs with mocha.
 */
describe('constants', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
