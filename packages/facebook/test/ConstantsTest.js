/**
 * Test for Constants.
 * Runs with mocha.
 */
'use strict'

describe('constants', () => {
  before(() => {})

  after(() => {})

  it('Do test', () => {})
})

/* global describe, before, after, it */
